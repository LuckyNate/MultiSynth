"use strict";
(function(global){
const MS=global.MultiSynth||{},E=MS.RackEngine,MF=MS.ModuleFaceplate,I=MS.ModuleIds,N=MS.LiveWireNative,world=document.getElementById("nodeWorld");if(!E||!MF||!world)return;
function findModule(id){for(const r of E.graph().racks||[]){const m=(r.modules||[]).find(x=>String(x.id)===String(id));if(m)return m}return null}
function liveWireScreen(body){body.innerHTML="";const screen=document.createElement("div");screen.className="nodeLiveWireScreen";const pic=document.createElement("div");pic.className="nodeLiveWirePicture";const scan=document.createElement("div");scan.className="nodeLiveWireScanlines";const badge=document.createElement("div");badge.className="nodeLiveWireBadge";badge.textContent="LIVE WIRE";screen.append(pic,scan,badge);body.appendChild(screen);
 const paint=s=>{const q=s?.queue||N?.queue||[],cur=q[0];if(cur?.id){pic.style.backgroundImage=`url(https://i.ytimg.com/vi/${cur.id}/hqdefault.jpg)`;badge.textContent=cur.title||"LIVE WIRE";screen.classList.toggle("playing",Number(s?.state)===1)}else{pic.style.backgroundImage="";badge.textContent="NO SIGNAL";screen.classList.remove("playing")}};paint(N?.playerState||{});N?.onPlayer?.(paint);}
function upgrade(card){if(!card||card.dataset.nodeKind!=="module"||card.dataset.moduleFaceplateReady==="1")return;const m=findModule(card.dataset.nodeId);if(!m)return;const i=MF.info(m.type),head=card.querySelector(".nodeHead"),body=card.querySelector(".nodeBody");card.classList.add("rackModuleIdentity",i.theme||"");card.style.setProperty("--module-color",i.color);if(head){head.classList.add("moduleHead");head.innerHTML=`<strong>${i.name.toUpperCase()}</strong><span>${String(i.description||i.category).toUpperCase()}</span><small>${String(i.category).toUpperCase()}</small>`}if(body){if(String(m.type)===String(I?.LIVE_WIRE||"live-wire")){card.classList.add("liveWireVisualNode");liveWireScreen(body)}else body.innerHTML=`<span class="miniStatus">MODULE</span>`}card.dataset.moduleFaceplateReady="1"}
function scan(){world.querySelectorAll('.nodeCard[data-node-kind="module"]').forEach(upgrade)}
new MutationObserver(scan).observe(world,{childList:true,subtree:true});E.on?.("graph-changed",()=>requestAnimationFrame(scan));scan();
})(window);
