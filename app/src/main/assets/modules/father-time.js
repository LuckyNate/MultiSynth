"use strict";
(function(global){
const MS=global.MultiSynth||{},C=MS.ModuleContract,I=MS.ModuleIds,Events=MS.Events;if(!C||!I)return;
const eventName=(key,fallback)=>Events?.[key]||fallback;
const dispatch=(key,fallback,detail)=>Events?.dispatch?Events.dispatch(eventName(key,fallback),detail):global.dispatchEvent(new CustomEvent(eventName(key,fallback),{detail}));
const clampBpm=v=>Math.max(20,Math.min(300,Number(v)||120));
function defaults(){return{bpm:120,running:false}}
function create(api){const u={ctx:api.context,unsubscribePulse:null,unsubscribeMidi:null,state:api.state};queueMicrotask(()=>{try{const runtime=C.getRuntime(api.instanceId);if(runtime.state?.running===true)attach(runtime)}catch(_){}});return u}
function detach(runtime){const u=runtime?.user;if(!u)return;for(const key of ["unsubscribePulse","unsubscribeMidi"]){if(!u[key])continue;try{u[key]()}catch(_){}u[key]=null}}
function attach(runtime){const u=runtime?.user,T=MS.PatchTransport;if(!u||!T||u.unsubscribePulse||u.unsubscribeMidi||runtime.state?.running!==true)return;u.unsubscribePulse=T.subscribeScheduledPulse?.(pulse=>{let live=null;try{live=C.getRuntime(runtime.instanceId)}catch(_){}if(!live||live.state?.running!==true)return;MS.NodeAudioGraph?.sendCV?.(runtime.instanceId,{kind:"trigger",clock:true,midiStatus:0xf8,origin:"patch-transport",value:1,gate:true,bpm:Number(pulse?.bpm)||Number(T.bpm)||120,pulse:Number(pulse?.pulse)||0,ppqn:24,time:Number(pulse?.time)||live.user?.ctx?.currentTime||0});if((Number(pulse?.pulse)||0)%24===0)dispatch("FATHER_TIME_CV_TRIGGER","multisynth-father-time-cv-trigger",{instanceId:runtime.instanceId,time:Number(pulse?.time)||0,pulse:Number(pulse?.pulse)||0,bpm:Number(pulse?.bpm)||Number(T.bpm)||120})})||null;u.unsubscribeMidi=T.subscribeMidi?.(event=>{if(event?.external)return;const status=Number(event?.status)&255;if(status===0xf8)global.MultiSynthNativeMidi?.sendClockPulse?.();else if(status===0xfa)global.MultiSynthNativeMidi?.sendStart?.();else if(status===0xfb)global.MultiSynthNativeMidi?.sendContinue?.();else if(status===0xfc)global.MultiSynthNativeMidi?.sendStop?.()})||null}
function setState({runtime,state,patch}){const u=runtime.user;if(u)u.state=state;const T=MS.PatchTransport;if(patch&&Object.prototype.hasOwnProperty.call(patch,"bpm")&&!T?.external)T?.setBpm?.(clampBpm(state.bpm));if(patch&&Object.prototype.hasOwnProperty.call(patch,"running")){if(state.running===true){attach(runtime);if(T&&!T.running&&!T.external)T.start()}else{if(T?.running&&!T.external)T.stop();detach(runtime)}}else if(state.running===true)attach(runtime)}
function cv(_ctx,packet={}){return packet}
function destroy({runtime}){detach(runtime)}
C.define({type:I.FATHER_TIME,version:"midi-clock-1",description:"PATCH MIDI CLOCK · 24 PPQN · CV CLOCK BRIDGE",defaults:defaults(),resources:["midi","storage"],dynamicPorts:{cvOut:"used-plus-one"},create,setState,cv,destroy,serialize:({state})=>({...state}),restore:({saved})=>Object.assign(defaults(),saved||{})});
C.defineSurface(I.FATHER_TIME,{version:3,package:{id:I.FATHER_TIME,version:3,behavior:{clock:"patch-midi-24-ppqn",cvClock:"midi-clock-pulse",usbMidi:"real-midi-realtime",audioMode:"none",cvOutputs:"used-plus-one",stateOwnership:"module"}},faceplate:{livery:"antique-clock",primary:"#21170f",secondary:"#8d6b45",tertiary:"#e7d3ad"},defaults:defaults(),controls:[{id:"bpm",control:"encoder",state:"bpm",label:"BPM",value:{default:120,min:30,max:300,step:1},meta:{visual:"clock-dial"},node:"controller.bpm"},{id:"pulse",control:"led",label:"CLOCK",meta:{source:"midi-clock"},node:"indicator.pulse"},{id:"running",control:"switch",state:"running",label:"RUN",node:"controller.running"}],sources:[{id:"source.patchMidi",type:"midiClock",mode:"24ppqn"}],actions:[{id:"action.transport",type:"midiRealtime"},{id:"action.cv",type:"cvClockBridge"}],nodes:{connections:[["source.patchMidi","action.transport"],["source.patchMidi","action.cv"],["action.cv","indicator.pulse"]]}})
})(window);
