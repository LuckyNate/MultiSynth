"use strict";
(function(global){
const C=global.MultiSynth?.ModuleContract;if(!C)return;
const types=["puresynth","quadsynth","pulsynth","sinladder","razorback","stinger","noquarter"];
const meta={puresynth:["PureSynth","puresynth.html","#f4f4f0","pure","MATHEMATICALLY PURE WAVEFORM ENGINE"],quadsynth:["QuadSynth","quadsynth.html","#ffb000","quad","CLICK · SINE · SAW · SQUARE"],pulsynth:["Pulsynth","pulsynth.html","#58ff78","pulse","THREE-STAGE PWM LADDER"],sinladder:["SinLadder","sinladder.html","#36eaff","sine","THREE-STAGE SINE HARMONIC LADDER"],razorback:["Razorback","razorback.html","#ff3d42","saw","TRIANGLE / MOVABLE-PEAK LADDER"],stinger:["Stinger","stinger.html","#ffe64a","stinger","POINTED SPINE CLICK LADDER"],noquarter:["No Quarter","noquarter.html","#77a4ff","noquarter","HAUNTED VELOCITY ELECTRIC PIANO"]};
const clamp=(v,a,b)=>Math.max(a,Math.min(b,Number(v)||0)),freq=n=>440*Math.pow(2,(Number(n)-69)/12);
function defaults(type){return{master:.35,level:.8,carrier:1,waveform:type==="puresynth"?"sine":null,pwm:50,peak:50,attack:.001,decay:.1,sustain:.75,release:.25,amount1:.35,amount2:.25,amount3:.20,acceleration1:88,acceleration2:92,acceleration3:96,phase1:0,phase2:120,phase3:240,octave1:0,octave2:0,octave3:0,detune1:0,detune2:0,detune3:0,direction1:"up",direction2:"up",direction3:"up"};}
function waveType(type,state){if(type==="pulsynth")return"square";if(type==="razorback")return"triangle";if(type==="stinger"||type==="quadsynth")return"sawtooth";if(type==="puresynth"){const w=state.waveform;return["sine","square","triangle","sawtooth"].includes(w)?w:(w==="saw"?"sawtooth":"sine");}return"sine";}
function stageAmount(s,n){return clamp(s["amount"+n]??([.35,.25,.20][n-1]),0,.85);}
function stageFreq(s,n,base){const oct=Number(s["octave"+n]||0),det=Number(s["detune"+n]||0);return Math.max(.1,base*Math.pow(2,oct+det/1200));}
function hasUpstream(runtime){try{return !!runtime.rack?.neighbors?.(runtime.instanceId)?.upstream?.length;}catch(_){return false;}}
function create(type,api){const c=api.context;if(!c)return{};const input=c.createGain(),dry=c.createGain(),wetIn=c.createGain(),mixer=c.createGain(),master=c.createGain(),safety=c.createDynamicsCompressor(),output=c.createGain();dry.gain.value=.25;wetIn.gain.value=.75;master.gain.value=clamp(api.state.master??.35,0,1);safety.threshold.value=-6;safety.knee.value=4;safety.ratio.value=12;safety.attack.value=.003;safety.release.value=.06;input.connect(dry);input.connect(wetIn);dry.connect(mixer);let node=wetIn;const stages=[];for(let i=0;i<3;i++){const g=c.createGain();g.gain.value=1;node.connect(g);node=g;stages.push(g);}node.connect(mixer);mixer.connect(master);master.connect(safety);safety.connect(output);api.setInput(input);api.setOutput(output);return{ctx:c,input,dry,wetIn,mixer,master,safety,output,stages,voices:new Map(),state:api.state,type};}
function stopVoice(v,when){if(!v)return;const t=when??v.ctx.currentTime;if(v.source)try{v.source.stop(t);}catch(_){}for(const m of v.mods)try{m.osc.stop(t);}catch(_){}setTimeout(()=>{try{v.source?.disconnect();v.sourceGain?.disconnect();}catch(_){}for(const m of v.mods)try{m.osc.disconnect();m.depth.disconnect();}catch(_){}},80);}
function noteOn({runtime,state},note,velocity=127){const u=runtime.user;if(!u?.ctx)return false;const k=String(note);if(u.voices.has(k))stopVoice(u.voices.get(k));const c=u.ctx,base=freq(note),vel=clamp(Number(velocity)/127,.001,1),fed=hasUpstream(runtime);let src=null,srcGain=null;if(!fed){src=c.createOscillator();srcGain=c.createGain();src.type=waveType(runtime.type,state);src.frequency.value=base;srcGain.gain.value=clamp((state.carrier??state.level??.8)*vel,0,.9);src.connect(srcGain);srcGain.connect(u.wetIn);src.start();}const mods=[];for(let n=1;n<=3;n++){const osc=c.createOscillator(),depth=c.createGain();osc.type=waveType(runtime.type,state);osc.frequency.value=stageFreq(state,n,base);depth.gain.value=stageAmount(state,n)*vel*.55;osc.connect(depth);depth.connect(u.stages[n-1].gain);osc.start();mods.push({osc,depth,n});}u.voices.set(k,{ctx:c,source:src,sourceGain:srcGain,mods,note:Number(note),velocity:vel,hasUpstream:fed});return true;}
function noteOff({runtime,state},note){const u=runtime.user,v=u?.voices.get(String(note));if(!v)return false;const rel=Math.max(.005,Number(state.release)||.05),now=u.ctx.currentTime;if(v.sourceGain)try{v.sourceGain.gain.cancelScheduledValues(now);v.sourceGain.gain.setTargetAtTime(.0001,now,Math.max(.003,rel/4));}catch(_){}for(const m of v.mods)try{m.depth.gain.setTargetAtTime(0,now,Math.max(.003,rel/4));}catch(_){}stopVoice(v,now+rel);u.voices.delete(String(note));return true;}
function panic({runtime}){const u=runtime.user;if(!u)return true;for(const v of u.voices.values())stopVoice(v);u.voices.clear();return true;}
function setState({runtime,state}){const u=runtime.user;if(!u?.ctx)return;u.state=state;u.master.gain.setTargetAtTime(clamp(state.master??.35,0,1),u.ctx.currentTime,.01);for(const v of u.voices.values()){const base=freq(v.note);for(const m of v.mods){m.osc.frequency.setTargetAtTime(stageFreq(state,m.n,base),u.ctx.currentTime,.01);m.depth.gain.setTargetAtTime(stageAmount(state,m.n)*v.velocity*.55,u.ctx.currentTime,.01);}}}
function destroy({runtime}){const u=runtime.user;if(!u)return;panic({runtime});for(const n of [u.input,u.dry,u.wetIn,...u.stages,u.mixer,u.master,u.safety,u.output])try{n.disconnect();}catch(_){}}
for(const type of types){const [displayName,editorUrl,color,selectorClass,description]=meta[type];C.define({type,displayName,category:"instrument",version:"rack-native-3",editorUrl,color,selectorClass,description,defaults:defaults(type),resources:["midi"],create:api=>create(type,api),setState,noteOn,noteOff,panic,destroy,serialize:({state})=>({...state}),restore:({saved})=>({...defaults(type),...(saved||{})})});}
})(window);
