"use strict";
(function(global){
const MS=global.MultiSynth||{},C=MS.ModuleContract,I=MS.ModuleIds,Defs=MS.ModuleBuilderDefinitions,Standard=MS.ModuleStandard,D=MS.DspSources;
if(!C||!I||!Defs||!Standard||!D)return;
const model=Defs.require(I.TIME_BANDITS),clamp=(v,a,b)=>Math.max(a,Math.min(b,Number(v)||0));
const VOICES={
  sub:{pitch:42,decay:1200,bend:-700,tone:28,character:65,level:88,kind:"sub"},
  kick:{pitch:58,decay:520,bend:-1500,tone:52,character:72,level:92,kind:"kick"},
  snare:{pitch:190,decay:360,bend:-250,tone:68,character:72,level:80,kind:"snare"},
  tom:{pitch:130,decay:720,bend:-380,tone:52,character:38,level:80,kind:"tom"},
  hat:{pitch:1250,decay:90,bend:0,tone:88,character:65,level:62,kind:"hatc"}
};
const defaultProbabilities=()=>Array(32).fill(100);
const defaults=()=>({...model.defaults,probabilities:Array.isArray(model.defaults.probabilities)?model.defaults.probabilities.slice(0,32):defaultProbabilities()});
function bpm(u){return clamp(u.state.bpm??120,30,300)}
function probabilities(u){const src=Array.isArray(u.state.probabilities)?u.state.probabilities:[];const fallback=clamp(u.state.probability??100,0,100);return Array.from({length:32},(_,i)=>clamp(src[i]??fallback,0,100))}
function periodFrom(u,p){const n=Number(p?.period);if(Number.isFinite(n)&&n>0)return n;const b=Number(p?.bpm);return 60/clamp(Number.isFinite(b)&&b>0?b:bpm(u),30,300)/4}
function timeFrom(u,p){const n=Number(p?.time);return Number.isFinite(n)&&n>0?n:u.ctx.currentTime+.001}
function dispatchStep(u,step,time,probability,hit){global.dispatchEvent(new CustomEvent("multisynth-time-bandits-step",{detail:{instanceId:u.id,step,time,probability,hit}}))}
function voice(u){const base=VOICES[String(u.state.drum||"kick")]||VOICES.kick;return{...base,pitch:clamp(u.state.tune??base.pitch,10,6000),decay:clamp(u.state.decay??base.decay,20,4000),level:clamp(u.state.level??base.level,0,100)}}
function envGain(c,level,t,dur,peak=1){const g=c.createGain();g.gain.setValueAtTime(Math.max(.0001,level*peak),t);g.gain.exponentialRampToValueAtTime(.0001,t+dur);return g}
function pitched(u,v,t,w="sine",amp=.7,bendScale=1,detune=0){const dur=Math.max(.02,v.decay/1000),base=Math.max(10,v.pitch),o=D.oscillator(u.ctx,w,base),g=envGain(u.ctx,v.level/100,t,dur,amp),bend=Number(v.bend||0)*bendScale,start=Math.max(10,base*Math.pow(2,-bend/1200));try{o.detune?.setValueAtTime(detune,t);o.frequency?.setValueAtTime(start,t);o.frequency?.exponentialRampToValueAtTime(base,t+Math.min(dur*.32,.12))}catch(_){}o.connect(g).connect(u.voice);o.start(t);o.stop(t+dur+.08)}
function filteredNoise(u,v,t,amp=.35,type="highpass",freq=null,durScale=1){const dur=Math.max(.025,v.decay/1000*durScale),s=D.noise(u.ctx,"white",dur,false),f=u.ctx.createBiquadFilter(),g=envGain(u.ctx,v.level/100,t,dur,amp);f.type=type;const tone=clamp(v.tone,0,100)/100;f.frequency.value=freq??(type==="lowpass"?400+tone*9000:300+tone*10500);f.Q.value=.5+clamp(v.character,0,100)/100*4;s.connect(f).connect(g).connect(u.voice);s.start(t);s.stop(t+dur)}
function transient(u,v,t,amp=.2){const c=u.ctx,n=Math.max(24,Math.floor(c.sampleRate*(.002+clamp(v.character,0,100)/100*.01))),b=D.shapedBuffer(c,n,(i,len)=>{const e=1-i/len;return(Math.random()*2-1)*e*e}),s=D.bufferSource(c,b),hp=c.createBiquadFilter(),g=envGain(c,v.level/100,t,.015,amp);hp.type="highpass";hp.frequency.value=1200+clamp(v.tone,0,100)/100*7000;s.connect(hp).connect(g).connect(u.voice);s.start(t)}
function metal(u,v,t,ratios,noiseAmp=.2,oscAmp=.05,type="square"){const ch=clamp(v.character,0,100)/100;for(let k=0;k<ratios.length;k++)pitched(u,{...v,pitch:v.pitch*ratios[k],level:v.level*(.42+ch*.18)},t,k%2?type:"triangle",oscAmp,0,(k-ratios.length/2)*6);if(noiseAmp)filteredNoise(u,v,t,noiseAmp,"highpass",1400+v.tone*90,1)}
function synthHit(u,t){const v=voice(u),ch=clamp(v.character,0,100)/100;if(v.kind==="sub"){pitched(u,v,t,"sine",.92,.7);if(ch>.55)pitched(u,{...v,pitch:v.pitch*2,decay:v.decay*.35,level:v.level*(ch-.45)},t,"sine",.18,.3)}else if(v.kind==="kick"){pitched(u,v,t,"sine",.95,1);transient(u,v,t,.08+ch*.24);if(ch>.72)filteredNoise(u,{...v,decay:Math.min(v.decay,90)},t,.08,"highpass",1800+v.tone*55,.25)}else if(v.kind==="snare"){pitched(u,v,t,"triangle",.28,.28,-7);pitched(u,{...v,pitch:v.pitch*1.62,decay:v.decay*.72},t,"sine",.18,.12,9);filteredNoise(u,v,t,.18+ch*.55,"highpass",900+v.tone*70,1);transient(u,v,t,.06+ch*.16)}else if(v.kind==="tom"){pitched(u,v,t,"sine",.76,.65);pitched(u,{...v,pitch:v.pitch*1.48,decay:v.decay*.55,level:v.level*(.35+ch*.25)},t,"triangle",.18,.22)}else if(v.kind==="hatc"){metal(u,v,t,[1,1.342,1.731,2.117,2.693,3.417],.16,.045);filteredNoise(u,v,t,.16,"highpass",3500+v.tone*75,.55)}}
function playSample(u,t){const buffer=u.sampleBuffer;if(!buffer)return false;const c=u.ctx,s=D.bufferSource(c,buffer),g=c.createGain();g.gain.value=clamp(u.state.level,0,100)/100;s.connect(g).connect(u.voice);s.onended=()=>{try{s.disconnect();g.disconnect()}catch(_){}};s.start(Math.max(c.currentTime+.001,Number(t)||c.currentTime+.001));return true}
function play(u,t){if(u.sampleBuffer)return playSample(u,t);synthHit(u,Math.max(u.ctx.currentTime+.001,Number(t)||u.ctx.currentTime+.001));return true}
async function load(u,key){u.sampleBuffer=null;if(!key)return;try{const r=await (MS.UnifiedLibrary||MS.PCMLibrary)?.get?.(key);if(!r?.data?.length)return;const b=u.ctx.createBuffer(1,r.data.length,r.sampleRate);b.getChannelData(0).set(r.data);u.sampleBuffer=b}catch(e){console.error("Time Bandits sample",e)}}
function emitCv(u,time,period,step,parent=null){MS.CvBus?.send(u.id,{kind:"trigger",value:1,gate:true,time,period,bpm:60/(period*4),source:u.id,parent,step,substep:step})}
function runStep(u,step,time,period,parent=null){const probs=probabilities(u),index=((Number(step)||0)%32+32)%32,p=probs[index],hit=Math.random()*100<p;u.step=index;dispatchStep(u,index,time,p,hit);if(!hit)return false;play(u,time);emitCv(u,time,period,index,parent);return true}
function clearFallback(u){if(u.fallbackTimer!==null)clearTimeout(u.fallbackTimer);u.fallbackTimer=null}
function armExternalFallback(u,p,quarter=false){clearFallback(u);const period=periodFrom(u,p),serial=++u.externalSerial;u.fallbackTimer=setTimeout(()=>{if(u.destroyed||serial!==u.externalSerial)return;u.external=false;u.transport.clockStop();u.fallbackTimer=null},Math.max(120,period*(quarter?5000:2500)))}
function takeExternal(u,p,quarter=false){if(!u.external){u.external=true;u.transport.clockStart()}const time=timeFrom(u,p),period=periodFrom(u,p);if(quarter){const start=(u.externalStep+1)%32;for(let n=0;n<4;n++)runStep(u,(start+n)%32,time+n*period,period,p?.source);u.externalStep=(start+3)%32}else{const step=(u.externalStep+1)%32;u.externalStep=step;runStep(u,step,time,period,p?.source)}armExternalFallback(u,p,quarter)}
function create(api){const c=api.context,input=c.createGain(),voiceNode=c.createGain(),mix=c.createGain(),output=c.createGain();input.connect(mix);voiceNode.connect(mix);mix.connect(output);api.setInput(input);api.setOutput(output);const u={id:api.instanceId,ctx:c,input,voice:voiceNode,mix,output,state:api.state,sampleBuffer:null,step:-1,externalStep:-1,external:false,externalSerial:0,fallbackTimer:null,destroyed:false,transport:null};u.state.probabilities=probabilities(u);if(api.state.pcmKey)load(u,api.state.pcmKey);u.transport=Standard.transport(c,{maxSteps:32,getState:()=>({running:true,bpm:bpm(u),steps:32}),onStep:(step,time)=>{if(!u.external)runStep(u,step,time,60/bpm(u)/4,u.id)}});u.transport.start();return u}
function setState({runtime,state,patch}){const u=runtime.user;if(!u)return;u.state=state;if("probabilities" in patch)u.state.probabilities=probabilities(u);if("pcmKey" in patch)load(u,state.pcmKey)}
function clockStart({runtime}){const u=runtime.user;if(!u)return;u.external=true;u.externalStep=-1;u.externalSerial++;clearFallback(u);u.transport.clockStart()}
function clockStop({runtime}){const u=runtime.user;if(!u)return;u.external=false;u.externalSerial++;clearFallback(u);u.transport.clockStop()}
function clockTick({runtime},tick={}){const u=runtime.user;if(!u)return false;takeExternal(u,tick,false);return true}
function cv({runtime},packet={}){const u=runtime.user;if(!u||packet?.kind!=="trigger"||packet.source===u.id)return null;takeExternal(u,packet,true);return null}
function destroy({runtime}){const u=runtime.user;if(!u)return;u.destroyed=true;u.externalSerial++;clearFallback(u);u.transport?.destroy?.();for(const n of[u.input,u.voice,u.mix,u.output])try{n.disconnect()}catch(_){}}
C.define({type:I.TIME_BANDITS,version:"module-builder-13",description:"32-STEP PROBABILITY DRUM · SINGLE BEAT RED SYNTH VOICE · EACH STEP HAS ITS OWN HIT CHANCE · CV TRIGGER CLOCK WITH BPM FALLBACK · SAMPLE ACCESS",defaults:defaults(),resources:["storage"],create,setState,clockStart,clockStop,clockTick,cv,destroy,moduleBuilder:model});
})(window);
