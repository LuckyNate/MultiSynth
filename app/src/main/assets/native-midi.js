(function () {
    "use strict";
    const held=new Map(),sustained=new Set(),sustainByChannel=new Array(16).fill(false);let runningStatus=null,pendingData=[],inSysEx=false;
    function key(channel,note){return channel+":"+note} function MS(){return window.MultiSynth||{}} function clone(v){if(v==null)return v;if(typeof structuredClone==="function")return structuredClone(v);return JSON.parse(JSON.stringify(v))}
    function emitCV(packet){const p=Object.assign({source:"usb-midi",transport:"midi",time:MS().NodeAudioGraph?.context?.currentTime||0},packet||{});window.dispatchEvent(new CustomEvent("multisynth-usb-cv",{detail:clone(p)}));return true}
    function sendNative(status,d1,d2,length){if(!window.AndroidMidi||typeof AndroidMidi.sendMidi!=="function")return false;try{return AndroidMidi.sendMidi(status&255,(d1||0)&127,(d2||0)&127,Math.max(1,Math.min(3,length||1)))!==false}catch(_){return false}}
    function maySendClock(){return MS().PatchTransport?.external!==true}
    function sendClockPulse(){return maySendClock()?sendNative(0xf8,0,0,1):false} function sendStart(){return maySendClock()?sendNative(0xfa,0,0,1):false} function sendContinue(){return maySendClock()?sendNative(0xfb,0,0,1):false} function sendStop(){return maySendClock()?sendNative(0xfc,0,0,1):false}
    function noteOnNative(channel,note,velocity){const id="native-midi:"+key(channel,note);held.set(key(channel,note),id);sustained.delete(key(channel,note));if(typeof noteOn==="function")noteOn(note,velocity,null,id);else MS().NodeAudioGraph?.noteOn?.(note,velocity);if(typeof setVisibleKey==="function")setVisibleKey(note,true);emitCV({kind:"trigger",gate:true,value:velocity/127,velocity,note,channel})}
    function release(channel,note){const k=key(channel,note),id=held.get(k)||"native-midi:"+k;held.delete(k);if(sustainByChannel[channel]){sustained.add(k);return}if(typeof noteOffByKey==="function")noteOffByKey(id);else MS().NodeAudioGraph?.noteOff?.(note);if(typeof refreshVisibleKey==="function")refreshVisibleKey(note);emitCV({kind:"gate",gate:false,value:0,note,channel})}
    function sustain(channel,down){sustainByChannel[channel]=down;emitCV({kind:"control",control:64,value:down?1:0,raw:down?127:0,channel});if(down)return;[...sustained].forEach(k=>{const p=k.split(":");if(+p[0]!==channel)return;sustained.delete(k);if(held.has(k))return;if(typeof noteOffByKey==="function")noteOffByKey("native-midi:"+k);else MS().NodeAudioGraph?.noteOff?.(+p[1]);if(typeof refreshVisibleKey==="function")refreshVisibleKey(+p[1]);emitCV({kind:"gate",gate:false,value:0,note:+p[1],channel})})}
    function realtime(status){const T=MS().PatchTransport;if(!T?.receiveMidi)return;const time=MS().NodeAudioGraph?.context?.currentTime??performance.now()/1000;T.receiveMidi(status,{time,source:"usb-midi"});if(status===0xf8){const bpm=Number(T.bpm);if(Number.isFinite(bpm))try{MS().NodeGraphEngine?.setPatchState?.({bpm})}catch(_){}}}
    function channelMessage(status,data){const command=status&0xf0,channel=status&0x0f,d1=data[0]&127,d2=(data[1]||0)&127;if(command===0x90&&d2>0)noteOnNative(channel,d1,d2);else if(command===0x80||(command===0x90&&d2===0))release(channel,d1);else if(command===0xb0){if(d1===64)sustain(channel,d2>=64);else if(d1===120||d1===123)panicChannel(channel);else emitCV({kind:"control",control:d1,value:d2/127,raw:d2,channel})}else if(command===0xe0){const raw=(d2<<7)|d1;emitCV({kind:"pitch",value:(raw-8192)/8192,raw,channel})}else if(command===0xd0)emitCV({kind:"pressure",value:d1/127,raw:d1,channel});else if(command===0xa0)emitCV({kind:"poly-pressure",value:d2/127,raw:d2,note:d1,channel});else if(command===0xc0)emitCV({kind:"program",value:d1,program:d1,channel})}
    function dataLength(status){const command=status&0xf0;return command===0xc0||command===0xd0?1:(command>=0x80&&command<=0xe0?2:0)}
    function process(bytes){for(const raw of bytes||[]){const byte=Number(raw)&255;if(byte>=0xf8){realtime(byte);continue}if(byte&0x80){pendingData=[];if(byte===0xf0){inSysEx=true;runningStatus=null;continue}if(byte===0xf7){inSysEx=false;runningStatus=null;continue}if(byte>=0xf1){inSysEx=false;runningStatus=null;continue}inSysEx=false;runningStatus=byte;continue}if(inSysEx||runningStatus===null)continue;pendingData.push(byte&127);const size=dataLength(runningStatus);if(!size){runningStatus=null;pendingData=[];continue}if(pendingData.length>=size){channelMessage(runningStatus,pendingData);pendingData=[]}}}
    function panicChannel(channel){[...held.keys(),...sustained].forEach(k=>{const p=k.split(":");if(+p[0]!==channel)return;held.delete(k);sustained.delete(k);if(typeof noteOffByKey==="function")noteOffByKey("native-midi:"+k);else MS().NodeAudioGraph?.noteOff?.(+p[1])});sustainByChannel[channel]=false;emitCV({kind:"panic",channel,gate:false,value:0})} function panicAll(){for(let c=0;c<16;c++)panicChannel(c);document.querySelectorAll(".key.down").forEach(k=>k.classList.remove("down"))}
    function installButton(){if(!window.AndroidMidi||document.getElementById("nativeMidiInput"))return;const controls=document.getElementById("connectionControls");if(!controls)return;const button=document.createElement("button");button.id="nativeMidiInput";button.className="btn";button.textContent="USB-C / MIDI";button.addEventListener("click",()=>{if(typeof ensureAudio==="function")ensureAudio();MS().NodeAudioGraph?.resume?.();AndroidMidi.chooseInput()});controls.appendChild(button);const audio=document.createElement("button");audio.className="btn";audio.textContent="AUDIO OUT";audio.addEventListener("click",()=>AndroidMidi.openAudioSettings());controls.appendChild(audio)}
    window.MultiSynthNativeMidi={receive:process,receiveCV:emitCV,sendClockPulse,sendStart,sendContinue,sendStop,get usbClockActive(){return MS().PatchTransport?.external===true},devicesChanged:function(){},permissionResult:function(){},status:function(text,connected){const b=document.getElementById("nativeMidiInput");if(!b)return;b.textContent=connected?"USB-C / MIDI ON":"USB-C / MIDI";b.title=text;b.classList.toggle("active",!!connected)},panic:panicAll};
    if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",installButton);else installButton();
})();
