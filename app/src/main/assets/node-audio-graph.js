"use strict";
(function(global){
const MS=global.MultiSynth=global.MultiSynth||{},E=()=>MS.NodeGraphEngine,C=()=>MS.ModuleContract,M=()=>MS.ModuleManifest,I=()=>MS.ModuleIds;
let ctx=null,collector=null,master=null,limiter=null,masterAnalyser=null,started=false,queued=false;const links=new Map(),collectorLinks=new Map(),routeFlags=new Map(),ribbonBaseFrequency=new WeakMap();
function ensureContext(){if(ctx)return ctx;const A=global.AudioContext||global.webkitAudioContext;if(!A)throw new Error("Web Audio unavailable");ctx=new A({latencyHint:"interactive"});collector=ctx.createGain();master=ctx.createGain();master.gain.value=1;limiter=ctx.createDynamicsCompressor();limiter.threshold.value=-3;limiter.ratio.value=20;masterAnalyser=ctx.createAnalyser();masterAnalyser.fftSize=1024;collector.connect(master);master.connect(limiter);limiter.connect(masterAnalyser);limiter.connect(ctx.destination);return ctx}
function caps(m){try{return M()?.get?.(m.type)?.capabilities||[]}catch(_){return[]}}
function has(m,cap){return caps(m).includes(cap)}
function runtime(m){try{return C().getRuntime(m.id)}catch(_){return E().createModuleRuntime(m.id,{audioContext:ensureContext(),native:null,node:{hasUpstream:false,hasDownstream:false}})}}
function connectRecord(map,key,a,b,signature=""){if(!a||!b||map.has(key))return;try{a.connect(b);map.set(key,{a,b,signature})}catch(e){console.error("Node audio link",e)}}
function disconnectRecord(map,key){const rec=map.get(key);if(!rec)return;try{rec.a.disconnect(rec.b)}catch(_){}map.delete(key)}
function syncNow(){queued=false;ensureContext();const g=E().graph(),by=new Map(g.modules.map(m=>[m.id,m])),incoming=new Set(),outgoing=new Set(),desired=new Map();for(const e of g.connections||[]){if(e.type!=="audio")continue;const a=E().parseNode(e.from),b=E().parseNode(e.to);if(a?.signal!=="carrier"||b?.signal!=="carrier")continue;const ma=by.get(a.id),mb=by.get(b.id);if(!ma||!mb)continue;incoming.add(b.id);outgoing.add(a.id);desired.set(e.id,{edge:e,a,b,ma,mb,signature:`${e.from}>${e.to}`})}
for(const key of [...links.keys()]){const d=desired.get(key),rec=links.get(key);if(!d||rec?.signature!==d.signature)disconnectRecord(links,key)}
for(const [key,d] of desired){if(links.has(key))continue;const ra=runtime(d.ma),rb=runtime(d.mb),target=d.b.index!=null?rb?.user?.input?.(d.b.index):rb?.input;connectRecord(links,key,ra?.output,target,d.signature)}
for(const m of g.modules){const rt=runtime(m),nextIn=incoming.has(m.id),nextOut=outgoing.has(m.id),prev=routeFlags.get(m.id);if(rt?.node){rt.node.hasUpstream=nextIn;rt.node.hasDownstream=nextOut}if(!prev||prev.in!==nextIn||prev.out!==nextOut){routeFlags.set(m.id,{in:nextIn,out:nextOut});try{C().update(m.id,{})}catch(_){}}}
for(const id of [...routeFlags.keys()])if(!by.has(id))routeFlags.delete(id);
const desiredMixers=new Set(g.modules.filter(m=>m.type===I()?.ALCHEMY_MIXER).map(m=>m.id));for(const id of [...collectorLinks.keys()])if(!desiredMixers.has(id))disconnectRecord(collectorLinks,id);for(const id of desiredMixers){if(collectorLinks.has(id))continue;const m=by.get(id),rt=m?runtime(m):null;connectRecord(collectorLinks,id,rt?.output,collector,id)}}
function rebuild(){if(queued)return;queued=true;queueMicrotask(syncNow)}
function start(){ensureContext();if(!started){E().on("graph-changed",rebuild);started=true}rebuild();return api}
function resume(){ensureContext();return ctx.state==="suspended"?ctx.resume():Promise.resolve()}
function eachNote(fn){for(const m of E().graph().modules)if(m.enabled!==false&&has(m,"noteInput")){runtime(m);try{fn(m)}catch(e){console.error(e)}}}
function isPureHold(m){return m?.type===I()?.PURE_SYNTH&&!!m?.state?.hold}
function refreshPureHold(){resume();let count=0;eachNote(m=>{if(!isPureHold(m))return;const rt=runtime(m),note=Number(rt?.user?.lastNote??60);C().panic(m.id);if(C().noteOn(m.id,note,127))count++});return count}
function setPureHold(enabled){let count=0;eachNote(m=>{if(m?.type!==I()?.PURE_SYNTH)return;const rt=runtime(m),note=Number(rt?.user?.lastNote??60);C().panic(m.id);if(enabled&&C().noteOn(m.id,note,127))count++});return count}
function retuneOscillator(osc,anchor,target,now){if(!osc?.frequency)return false;try{let base=ribbonBaseFrequency.get(osc);if(!Number.isFinite(base)){base=Number(osc.frequency.value);if(!Number.isFinite(base)||base<=0)return false;ribbonBaseFrequency.set(osc,base)}const ratio=Math.pow(2,(target-anchor)/12),hz=Math.max(.01,base*ratio);osc.frequency.setTargetAtTime(hz,now,.006);return true}catch(_){return false}}
function retuneNote(anchor,target){const a=Number(anchor),t=Number(target);if(!Number.isFinite(a)||!Number.isFinite(t))return 0;let count=0;eachNote(m=>{const rt=runtime(m),voices=rt?.user?.voices;if(!voices||typeof voices.get!=="function")return;const voice=voices.get(String(a));if(!voice)return;const now=rt?.context?.currentTime??ctx?.currentTime??0;let changed=false;for(const osc of voice.sources||[])if(retuneOscillator(osc,a,t,now))changed=true;for(const mod of voice.mods||[])if(retuneOscillator(mod?.osc,a,t,now))changed=true;if(changed){voice.__ribbonPitch=t;count++}});return count}
const api=Object.freeze({start,rebuild,resume,noteOn:(n,v=127)=>{resume();let count=0;eachNote(m=>{if(isPureHold(m))C().panic(m.id);if(C().noteOn(m.id,n,v))count++});return count},noteOff:n=>{let count=0;eachNote(m=>{if(isPureHold(m))return;if(C().noteOff(m.id,n))count++});return count},setPureHold,refreshPureHold,retuneNote,panic:()=>eachNote(m=>C().panic(m.id)),sendCV:(source,packet)=>MS.CvBus?.send?.(source,packet),setMaster:v=>{ensureContext();master.gain.setTargetAtTime(Math.max(0,Math.min(1,Number(v)||0)),ctx.currentTime,.01)},get context(){return ensureContext()},get collector(){ensureContext();return collector},get masterAnalyser(){ensureContext();return masterAnalyser}});
MS.NodeAudioGraph=api;
})(window);