"use strict";

const BASE_DELAY_SECONDS = 15;
const MAX_LOOP_SECONDS = 60;
const MAX_DELAY_SECONDS = 62;

let ctx=null,micStream=null,micSource=null,inputGain=null,delay=null,tapeFilter=null,tapeSaturation=null,feedbackGain=null,dryGain=null,echoGain=null,outputGain=null,analyser=null,wowOsc=null,wowDepth=null,flutterOsc=null,flutterDepth=null,hissSource=null,hissGain=null,running=false,drawHandle=0;

const ids=["inputLevel","tapeSpeed","feedback","dryLevel","echoLevel","wow","flutter","wear","outputLevel"];
const controls=Object.fromEntries(ids.map(id=>[id,document.getElementById(id)]));
const statusEl=document.getElementById("status"),runButton=document.getElementById("runButton"),speedReadout=document.getElementById("speedReadout"),feedbackReadout=document.getElementById("feedbackReadout"),scope=document.getElementById("scope"),scopeCtx=scope.getContext("2d");
function value(id){return Number(controls[id].value)}
function pct(v){return `${Math.round(v*100)}%`}
// Gentle taper: every control moves more gradually while preserving its full endpoint.
function soft01(x){x=Math.max(0,Math.min(1,x));return x*x}
function softRange(id){const el=controls[id],min=Number(el.min)||0,max=Number(el.max)||1,x=(value(id)-min)/(max-min);return min+(max-min)*soft01(x)}
function speedValue(){const raw=value("tapeSpeed"),center=1;if(raw===center)return center;if(raw<center){const x=(center-raw)/(center-.25);return center-(center-.25)*soft01(x)}const x=(raw-center)/(4-center);return center+(4-center)*soft01(x)}
function loopSeconds(){return Math.min(MAX_LOOP_SECONDS,BASE_DELAY_SECONDS/Math.max(.25,speedValue()))}
function updateReadouts(){
 document.getElementById("inputValue").textContent=pct(softRange("inputLevel"));
 document.getElementById("speedValue").textContent=`${speedValue().toFixed(2)}×`;
 document.getElementById("feedbackValue").textContent=pct(softRange("feedback"));
 document.getElementById("dryValue").textContent=pct(softRange("dryLevel"));
 document.getElementById("echoValue").textContent=pct(softRange("echoLevel"));
 document.getElementById("wowValue").textContent=pct(softRange("wow"));
 document.getElementById("flutterValue").textContent=pct(softRange("flutter"));
 document.getElementById("wearValue").textContent=pct(softRange("wear"));
 document.getElementById("outputValue").textContent=pct(softRange("outputLevel"));
 const seconds=loopSeconds();speedReadout.textContent=`${speedValue().toFixed(2)}× // ${seconds>=10?seconds.toFixed(1):seconds.toFixed(2)} s LOOP`;feedbackReadout.textContent=`FEEDBACK ${Math.round(softRange("feedback")*100)}%`;
}
function makeSaturationCurve(amount){const n=2048,curve=new Float32Array(n),drive=1+amount*10,norm=Math.tanh(drive);for(let i=0;i<n;i++){const x=i*2/(n-1)-1;curve[i]=Math.tanh(x*drive)/norm}return curve}
function makeNoiseBuffer(context){const length=context.sampleRate*2,buffer=context.createBuffer(1,length,context.sampleRate),data=buffer.getChannelData(0);for(let i=0;i<length;i++)data[i]=Math.random()*2-1;return buffer}
function applyDSP(immediate=false){updateReadouts();saveState();if(!ctx)return;const now=ctx.currentTime,tau=immediate?.001:.04;inputGain.gain.setTargetAtTime(softRange("inputLevel"),now,tau);dryGain.gain.setTargetAtTime(softRange("dryLevel"),now,tau);echoGain.gain.setTargetAtTime(softRange("echoLevel"),now,tau);feedbackGain.gain.setTargetAtTime(softRange("feedback"),now,tau);outputGain.gain.setTargetAtTime(softRange("outputLevel"),now,tau);const loopTime=loopSeconds();delay.delayTime.setTargetAtTime(loopTime,now,.14);const wow=softRange("wow"),flutter=softRange("flutter"),wear=softRange("wear");wowDepth.gain.setTargetAtTime(wow*Math.min(loopTime*.018,.9),now,.06);flutterDepth.gain.setTargetAtTime(flutter*Math.min(loopTime*.004,.18),now,.06);tapeFilter.frequency.setTargetAtTime(18000-wear*15000,now,.06);tapeFilter.Q.setTargetAtTime(.15+wear*.35,now,.06);tapeSaturation.curve=makeSaturationCurve(wear);hissGain.gain.setTargetAtTime(wear*.012,now,.08)}
async function startTape(){if(running)return stopTape();try{const A=window.AudioContext||window.webkitAudioContext;ctx=new A({latencyHint:"interactive"});await ctx.resume();micStream=await navigator.mediaDevices.getUserMedia({audio:{channelCount:1,echoCancellation:false,noiseSuppression:false,autoGainControl:false}});micSource=ctx.createMediaStreamSource(micStream);inputGain=ctx.createGain();delay=ctx.createDelay(MAX_DELAY_SECONDS);tapeFilter=ctx.createBiquadFilter();tapeFilter.type="lowpass";tapeSaturation=ctx.createWaveShaper();tapeSaturation.oversample="2x";feedbackGain=ctx.createGain();dryGain=ctx.createGain();echoGain=ctx.createGain();outputGain=ctx.createGain();analyser=ctx.createAnalyser();analyser.fftSize=1024;micSource.connect(inputGain);inputGain.connect(dryGain);dryGain.connect(outputGain);inputGain.connect(delay);delay.connect(tapeFilter);tapeFilter.connect(tapeSaturation);tapeSaturation.connect(echoGain);echoGain.connect(outputGain);tapeSaturation.connect(feedbackGain);feedbackGain.connect(delay);wowOsc=ctx.createOscillator();wowOsc.type="sine";wowOsc.frequency.value=.55;wowDepth=ctx.createGain();wowOsc.connect(wowDepth);wowDepth.connect(delay.delayTime);wowOsc.start();flutterOsc=ctx.createOscillator();flutterOsc.type="sine";flutterOsc.frequency.value=7.3;flutterDepth=ctx.createGain();flutterOsc.connect(flutterDepth);flutterDepth.connect(delay.delayTime);flutterOsc.start();hissSource=ctx.createBufferSource();hissSource.buffer=makeNoiseBuffer(ctx);hissSource.loop=true;hissGain=ctx.createGain();hissSource.connect(hissGain);hissGain.connect(tapeFilter);hissSource.start();outputGain.connect(analyser);analyser.connect(ctx.destination);running=true;runButton.textContent="STOP TAPE";runButton.classList.add("active");statusEl.textContent="TAPE RUNNING";applyDSP(true);drawScope()}catch(error){console.error(error);statusEl.textContent="INPUT ERROR";await stopTape()}}
async function stopTape(){running=false;cancelAnimationFrame(drawHandle);try{if(wowOsc)wowOsc.stop()}catch(_){}try{if(flutterOsc)flutterOsc.stop()}catch(_){}try{if(hissSource)hissSource.stop()}catch(_){}if(micStream)micStream.getTracks().forEach(track=>track.stop());if(ctx&&ctx.state!=="closed")await ctx.close();ctx=micStream=micSource=inputGain=delay=tapeFilter=tapeSaturation=null;feedbackGain=dryGain=echoGain=outputGain=analyser=null;wowOsc=wowDepth=flutterOsc=flutterDepth=hissSource=hissGain=null;runButton.textContent="START TAPE";runButton.classList.remove("active");if(statusEl.textContent!=="INPUT ERROR")statusEl.textContent="STOPPED";clearScope()}
function resizeScope(){const dpr=window.devicePixelRatio||1,rect=scope.getBoundingClientRect();scope.width=Math.max(1,Math.floor(rect.width*dpr));scope.height=Math.max(1,Math.floor(rect.height*dpr));scopeCtx.setTransform(dpr,0,0,dpr,0,0);if(!running)clearScope()}
function clearScope(){const w=scope.clientWidth,h=scope.clientHeight;scopeCtx.fillStyle="#fff4ef";scopeCtx.fillRect(0,0,w,h);scopeCtx.strokeStyle="#f58ab3";scopeCtx.lineWidth=1;scopeCtx.beginPath();scopeCtx.moveTo(0,h/2);scopeCtx.lineTo(w,h/2);scopeCtx.stroke()}
function drawScope(){if(!running||!analyser)return;const data=new Uint8Array(analyser.fftSize);analyser.getByteTimeDomainData(data);const w=scope.clientWidth,h=scope.clientHeight;scopeCtx.fillStyle="#fff4ef";scopeCtx.fillRect(0,0,w,h);scopeCtx.strokeStyle="#ffd84a";scopeCtx.lineWidth=3;scopeCtx.beginPath();for(let i=0;i<data.length;i++){const x=i/(data.length-1)*w,y=data[i]/255*h;i===0?scopeCtx.moveTo(x,y):scopeCtx.lineTo(x,y)}scopeCtx.stroke();scopeCtx.strokeStyle="#c83f78";scopeCtx.lineWidth=1;scopeCtx.beginPath();scopeCtx.moveTo(0,h/2);scopeCtx.lineTo(w,h/2);scopeCtx.stroke();drawHandle=requestAnimationFrame(drawScope)}
function saveState(){const state={};ids.forEach(id=>state[id]=controls[id].value);try{localStorage.setItem("tapeworm-state",JSON.stringify(state))}catch(_){}}
function loadState(){try{const state=JSON.parse(localStorage.getItem("tapeworm-state")||"null");if(state)ids.forEach(id=>{if(state[id]!==undefined)controls[id].value=state[id]})}catch(_){}}
ids.forEach(id=>controls[id].addEventListener("input",()=>applyDSP(false)));runButton.addEventListener("click",startTape);window.addEventListener("resize",resizeScope);window.addEventListener("pagehide",()=>{if(running)stopTape()});loadState();updateReadouts();requestAnimationFrame(resizeScope);
