"use strict";
(function(){
const MS=window.MultiSynth||{},M=MS.ModuleManifest,H=MS.HierarchyLibrary,E=MS.RackEngine,P=()=>window.MultiSynthNodePlane;
const shell=document.getElementById("nodeBrowser"),items=document.getElementById("nodeBrowserItems"),mods=document.getElementById("browseModules"),racks=document.getElementById("browseRacks"),viewport=document.getElementById("nodeViewport");let tab="modules",insertPoint=null,holdTimer=null,holdStart=null;
if(!shell||!items||!viewport)return;
function nameForRack(id){try{return H?.list("rack")?.find(x=>String(x.id)===String(id))?.name||`Rack ${String(id).slice(-6).toUpperCase()}`}catch(_){return`Rack ${String(id).slice(-6).toUpperCase()}`}}
function close(){shell.classList.add("hidden")}
function openAt(clientX,clientY){const api=P();if(!api)return;insertPoint=api.worldPoint(clientX,clientY);shell.classList.remove("hidden");render();try{navigator.vibrate?.(18)}catch(_){}}
function render(){items.innerHTML="";mods?.classList.toggle("active",tab==="modules");racks?.classList.toggle("active",tab==="racks");if(tab==="modules"){for(const m of M?.all||[]){const b=document.createElement("button");b.className=`nodeBrowserItem moduleChoice synthMini ${m.themeKey||""}`.trim();b.style.setProperty("--module-color",m.color||"#789");b.innerHTML=`<span class="miniStatus">ADD</span><strong>${m.displayName.toUpperCase()}</strong><span class="miniDesc">${String(m.category||"MODULE").toUpperCase()}</span>`;b.onclick=()=>{const api=P();if(!api)return;try{api.addModuleType(m.id,insertPoint);close()}catch(e){api.flash?.(e.message||"COULD NOT ADD MODULE")}};items.appendChild(b)}}else{const gs=(E?.graph?.().racks||[]).filter(r=>!String(r.id).startsWith("nodehost-"));if(!gs.length){const e=document.createElement("div");e.className="nodeBrowserEmpty";e.textContent="NO SAVED RACKS";items.appendChild(e)}for(const r of gs){const b=document.createElement("button");b.className="nodeBrowserItem rackFaceplate";b.innerHTML=`<span class="miniStatus">RACK</span><strong>${nameForRack(r.id).toUpperCase()}</strong><span class="miniDesc">${(r.modules||[]).length} MODULE${(r.modules||[]).length===1?"":"S"}</span>`;b.onclick=()=>{const api=P();if(!api)return;api.placeRack(r.id,insertPoint);close()};items.appendChild(b)}}}
function cancelHold(){if(holdTimer){clearTimeout(holdTimer);holdTimer=null}holdStart=null}
viewport.addEventListener("pointerdown",e=>{if(e.target.closest(".nodeCard,.nodePort"))return;holdStart={id:e.pointerId,x:e.clientX,y:e.clientY};holdTimer=setTimeout(()=>{if(holdStart){openAt(holdStart.x,holdStart.y);cancelHold()}},550)},true);
viewport.addEventListener("pointermove",e=>{if(!holdStart||e.pointerId!==holdStart.id)return;if(Math.hypot(e.clientX-holdStart.x,e.clientY-holdStart.y)>12)cancelHold()},true);
viewport.addEventListener("pointerup",cancelHold,true);viewport.addEventListener("pointercancel",cancelHold,true);
document.getElementById("closeNodeBrowser")?.addEventListener("click",close);mods?.addEventListener("click",()=>{tab="modules";render()});racks?.addEventListener("click",()=>{tab="racks";render()});
})();
