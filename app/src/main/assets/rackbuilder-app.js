"use strict";
(function(global){
const E=global.MultiSynth.RackEngine,B=global.MultiSynth.RackBuilder,C=global.MultiSynth.ModuleContract;
const grid=document.getElementById("rackGrid"),rackList=document.getElementById("rackList"),cascadePanel=document.getElementById("cascadePanel"),cascadeList=document.getElementById("cascadeList"),cascadeTitle=document.getElementById("cascadeTitle"),neighborhoodText=document.getElementById("neighborhoodText"),toast=document.getElementById("toast");
let selected=null,audioCtx=null,openModule=null;
const ROWS=12,COLS=9;
function ensureAudio(){if(!audioCtx){const A=global.AudioContext||global.webkitAudioContext;if(!A)throw new Error("Web Audio unavailable");audioCtx=new A({latencyHint:"interactive"});}if(audioCtx.state==="suspended")audioCtx.resume().catch(()=>{});return audioCtx;}
function rackName(r){return `RACK ${r.row+1}.${r.col+1}`;}
function flash(msg){toast.textContent=msg;toast.classList.add("show");setTimeout(()=>toast.classList.remove("show"),1400);}
function buildGrid(){grid.innerHTML="";for(let r=0;r<ROWS;r++)for(let c=0;c<COLS;c++){const cell=document.createElement("div");cell.className="rackCell";cell.dataset.rackRow=r;cell.dataset.rackCol=c;grid.appendChild(cell);}render();}
function render(){const graph=E.graph();grid.querySelectorAll(".rackNode").forEach(n=>n.remove());for(const r of graph.racks){const cell=[...grid.children].find(x=>+x.dataset.rackRow===r.row&&+x.dataset.rackCol===r.col);if(!cell)continue;const node=document.createElement("div");node.className="rackNode"+(r.id===selected?" selected":"");node.dataset.rackId=r.id;node.innerHTML=`<strong>${rackName(r)}</strong><span>${r.modules.length} COMPONENT${r.modules.length===1?"":"S"}</span>`;cell.appendChild(node);}applyNeighborhoodClasses();renderRackList();if(selected)renderCascade();}
function applyNeighborhoodClasses(){grid.querySelectorAll(".rackNode").forEach(n=>n.classList.remove("rack-parent","rack-sibling","rack-child"));if(!selected)return;for(const [id,cls] of B.neighborhoodClasses(selected)){grid.querySelector(`[data-rack-id="${CSS.escape(id)}"]`)?.classList.add(cls);}}
function renderRackList(){rackList.innerHTML="";const racks=E.graph().racks.sort((a,b)=>a.row-b.row||a.col-b.col);if(!racks.length){rackList.innerHTML='<div class="rackListItem"><span>Tap any empty grid cell to create the first rack.</span></div>';return;}for(const r of racks){const item=document.createElement("button");item.type="button";item.className="rackListItem"+(r.id===selected?" selected":"");item.innerHTML=`<strong>${rackName(r)}</strong><span>ROW ${r.row} · COL ${r.col} · ${r.modules.length} COMPONENTS</span>`;item.onclick=()=>openRack(r.id);rackList.appendChild(item);}}
function openRack(id){selected=id;openModule=null;B.state.selectedRack=id;cascadePanel.classList.remove("hidden");render();cascadePanel.scrollIntoView({behavior:"smooth",block:"nearest"});}
function createRack(row,col){try{const id=E.addRack(row,col);openRack(id);}catch(err){flash(err.message);}}
function ensureRuntime(rackId,m){try{return C.getRuntime(m.id);}catch(_){return E.createModuleRuntime(rackId,m.id,{audioContext:ensureAudio(),native:global.MultiSynth?.rack?.native||null});}}
function moduleCard(r,m){const card=document.createElement("div");card.className="moduleCard"+(openModule===m.id?" open":"");card.dataset.moduleId=m.id;card.innerHTML=`<div class="moduleHead"><strong>${(m.displayName||m.type).toUpperCase()}</strong><span>${m.type} · INSTANCE ${m.id.slice(-5)}</span></div><div class="moduleControls" data-controls></div>`;if(openModule===m.id){const runtime=ensureRuntime(r.id,m);C.mount(m.id,card.querySelector("[data-controls]"));}return card;}
function renderCascade(){if(!selected)return;let r;try{r=E.getRack(selected);}catch(_){selected=null;cascadePanel.classList.add("hidden");return;}cascadeTitle.textContent=rackName(r);const n=E.neighborhood(selected);neighborhoodText.textContent=`PARENTS ${n.parents.length}/3 · SIBLINGS ${n.siblings.length}/2 · CHILDREN ${n.children.length}/3`;cascadeList.innerHTML="";if(!r.modules.length){const empty=document.createElement("div");empty.className="moduleCard";empty.innerHTML="<strong>EMPTY CASCADE</strong><span>ADD A COMPONENT BELOW</span>";cascadeList.appendChild(empty);}else for(const m of r.modules)cascadeList.appendChild(moduleCard(r,m));B.bindCascade(cascadeList,selected,{onOpenModule:(_,mid)=>{openModule=openModule===mid?null:mid;renderCascade();}});}
function addComponent(){if(!selected)return;const defs=E.listModuleDefinitions();if(!defs.length){flash("NO MODULE DEFINITIONS");return;}const pure=defs.find(d=>d.type==="puresynth")||defs[0];try{const mid=E.addModule(selected,pure.type);ensureRuntime(selected,E.getRack(selected).modules.find(m=>m.id===mid));openModule=mid;render();flash(`${pure.displayName.toUpperCase()} ADDED`);}catch(err){flash(err.message);}}
B.bindGrid(grid,{onOpenRack:openRack,onEmptyCell:createRack,onPreview:()=>{}});E.on("graph-changed",render);window.addEventListener("multisynth-rack-ui-error",e=>flash(e.detail));
document.getElementById("closeCascade").onclick=()=>{selected=null;openModule=null;B.state.selectedRack=null;cascadePanel.classList.add("hidden");render();};
document.getElementById("newRackBtn").onclick=()=>{for(let r=0;r<ROWS;r++)for(let c=0;c<COLS;c++)if(!E.rackAt(r,c)){createRack(r,c);return;}flash("GRID FULL");};
document.getElementById("addModuleBtn").onclick=addComponent;
buildGrid();
})(window);
