"use strict";
(function(global){
const localMS=global.MultiSynth||{},q=new URLSearchParams(location.search),embeddedGraph=q.get("embeddedGraph")==="1"&&global.parent&&global.parent!==global,parentMS=embeddedGraph?(global.parent.MultiSynth||{}):null,MS=embeddedGraph?parentMS:localMS;
if(embeddedGraph){
  if(parentMS?.RackEngine)localMS.RackEngine=parentMS.RackEngine;
  if(parentMS?.ModuleContract)localMS.ModuleContract=parentMS.ModuleContract;
  if(parentMS?.RackAudioGraph)localMS.RackAudioGraph=parentMS.RackAudioGraph;
}
const E=MS.RackEngine,B=localMS.RackBuilder,C=MS.ModuleContract,A=MS.RackAudioGraph,M=localMS.ModuleManifest||MS.ModuleManifest,D=localMS.ControlDescriptors||MS.ControlDescriptors,S=localMS.ModuleSelectorUI||MS.ModuleSelectorUI;
const STORAGE="multisynth.rack.project.v1",list=document.getElementById("moduleOrder"),title=document.getElementById("rackTitle"),routeStatus=document.getElementById("rackRouteStatus"),chooser=document.getElementById("moduleChooser"),choices=document.getElementById("moduleChoices"),editor=document.getElementById("moduleEditor"),editorFrame=document.getElementById("moduleEditorFrame"),editorTitle=document.getElementById("moduleEditorTitle"),toast=document.getElementById("toast"),nameEditor=document.getElementById("rackNameEditor"),nameInput=document.getElementById("rackNameInput");
let rackId=q.get("selected")||q.get("rack")||"",audioStarted=false,restoring=true,editing=null,persistTimer=null;
const metaFor=t=>M?.get(t)||null,flash=s=>{if(!toast)return;toast.textContent=s;toast.classList.add("show");setTimeout(()=>toast.classList.remove("show"),1300)};
function persistNow(){if(restoring)return;if(persistTimer){clearTimeout(persistTimer);persistTimer=null}try{localStorage.setItem(STORAGE,E.serialize({selectedRack:rackId,workspaceType:"rack",routing:"explicit-nodes",at:Date.now()}))}catch(e){console.error("Rack save",e)}}
function persist(){if(restoring)return;if(persistTimer)clearTimeout(persistTimer);persistTimer=setTimeout(persistNow,300)}
function restore(){
  if(embeddedGraph){
    if(!rackId||!E.graph().racks.some(r=>r.id===rackId))throw new Error("Embedded rack editor target not found: "+rackId);
    restoring=false;
    return;
  }
  try{const raw=localStorage.getItem(STORAGE);if(raw){const meta=E.restore(raw)||{};if(!rackId)rackId=meta.selectedRack||""}}catch(e){console.error("Rack restore",e)}if(!rackId||!E.graph().racks.some(r=>r.id===rackId)){const slot=E.graph().racks.length;try{rackId=E.addRack(900000,900000+slot)}catch(_){rackId=E.graph().racks[0]?.id||""}}restoring=false;persistNow()}
function ensureAudio(){if(!A)throw new Error("Rack audio graph unavailable");if(!audioStarted){A.start();audioStarted=true}A.resume().catch(()=>{});return A.context}
function ensureRuntime(m){try{return C.getRuntime(m.id)}catch(_){return E.createModuleRuntime(rackId,m.id,{audioContext:ensureAudio(),native:MS.rack?.native||null})}}
function controlValue(el){if(el.type==="checkbox"||el.type==="radio")return!!el.checked;const n=Number(el.value);return el.type==="range"||el.type==="number"?n:el.value}
function controlPatch(el){if(!D||!el?.id)return{[el.id]:controlValue(el)};const type=el.type==="checkbox"||el.type==="radio"?D.TOGGLE:el.tagName==="SELECT"?D.SELECT:D.RANGE;try{return D.write(D.normalize({type,key:el.id,elementId:el.id,label:el.getAttribute("aria-label")||el.id}),controlValue(el))}catch(_){return{[el.id]:controlValue(el)}}}
function patchModule(mid,patch){E.setModuleState(rackId,mid,patch);persist()}
function bindEditor(doc,m){if(!doc)return;doc.documentElement.dataset.rackEmbedded="1";doc.body?.setAttribute("data-rack-embedded","1");const sync=el=>{if(el?.id)patchModule(m.id,controlPatch(el))};doc.addEventListener("input",e=>sync(e.target),true);doc.addEventListener("change",e=>sync(e.target),true);try{const rt=ensureRuntime(m),state=rt.state||{};doc.querySelectorAll("input[id],select[id],textarea[id]").forEach(el=>{if(!(el.id in state))return;if(el.type==="checkbox"||el.type==="radio")el.checked=!!state[el.id];else el.value=state[el.id]})}catch(e){console.error(e)}}
function openModule(mid){persistNow();ensureAudio();const r=E.getRack(rackId),m=r.modules.find(x=>x.id===mid);if(!m)return;const def=C.getDefinition(m.type),meta=metaFor(m.type),url=meta?.editorUrl||def.editorUrl;if(!url){flash("NO MODULE EDITOR");return}ensureRuntime(m);editing=mid;editorTitle.textContent=(meta?.displayName||m.displayName||m.type).toUpperCase();editor.classList.remove("hidden");const params=new URLSearchParams({rack:rackId,instance:m.id,embedded:"1"});editorFrame.onload=()=>{try{bindEditor(editorFrame.contentDocument,m)}catch(e){console.error(e)}};editorFrame.src=url+"?"+params}
function closeEditor(){if(!editing)return;editing=null;editorFrame.src="about:blank";editor.classList.add("hidden");persistNow();render()}
function moduleCard(m){const def=C.getDefinition(m.type),meta=metaFor(m.type),name=meta?.displayName||m.displayName||m.type,theme=meta?.themeKey||def.selectorClass||"",color=meta?.color??def.color,card=document.createElement("div");card.className=`moduleCard rackModuleIdentity ${theme}`.trim();card.dataset.moduleId=m.id;if(color)card.style.setProperty("--module-color",color);card.innerHTML=`<div class="moduleHead"><strong>${name.toUpperCase()}</strong><span>${def.description||meta?.category||"MODULE"}</span><small>INSTANCE ${m.id.slice(-5)}</small></div>`;return card}
function currentRackName(){const r=E.getRack(rackId);return String(r.name||r.id)}
function render(){let r;try{r=E.getRack(rackId)}catch(_){return}title.textContent=String(r.name||r.id).toUpperCase();const incoming=(E.graph().connections||[]).filter(c=>c.to===E.rackIn(rackId)).length,outgoing=(E.graph().connections||[]).filter(c=>c.from===E.rackOut(rackId)).length;routeStatus.textContent=`NODE GRAPH · IN ${incoming} · OUT ${outgoing} · EXTERNAL ROUTING LIVES ON THE GRAPH`;list.innerHTML="";if(!r.modules.length){const e=document.createElement("div");e.className="moduleCard";e.innerHTML="<strong>EMPTY RACK</strong><span>ADD A MODULE BELOW</span>";list.appendChild(e)}else for(const m of r.modules)list.appendChild(moduleCard(m));B.bindModuleOrder(list,rackId,{onOpenModule:(_,mid)=>openModule(mid)})}
function showNameEditor(){if(!nameEditor||!nameInput)return;nameInput.value=currentRackName();nameEditor.classList.remove("hidden");nameInput.focus();nameInput.select()}
function hideNameEditor(){nameEditor?.classList.add("hidden")}
function saveName(){const next=String(nameInput?.value||"").trim();E.renameRack(rackId,next);hideNameEditor();render();persistNow();try{global.parent?.MultiSynthNodePlane?.render?.()}catch(_){}flash(next?"RACK NAME SAVED":"RACK NAME RESET TO ID")}
function addPickedModule(m){try{const mid=E.addModule(rackId,m.id),mod=E.getRack(rackId).modules.find(z=>z.id===mid);ensureRuntime(mod);chooser.classList.add("hidden");persistNow();render();flash(`${String(m.displayName||m.id).toUpperCase()} ADDED`)}catch(e){flash(e.message)}}
function showChooser(){if(!S?.mount){flash("MODULE SELECTOR UNAVAILABLE");return}S.mount(choices,{modules:M?.all||[],onPick:addPickedModule});chooser.classList.remove("hidden")}
if(!E||!B||!C){flash("RACK ENGINE LOAD FAILED");return}try{restore()}catch(e){console.error(e);flash(e.message||"RACK LOAD FAILED");return}render();E.on("graph-changed",()=>{persistNow();render()});E.on("module-state",persist);document.getElementById("addModuleBtn").onclick=showChooser;document.getElementById("closeModuleChooser").onclick=()=>chooser.classList.add("hidden");document.getElementById("closeModuleEditor").onclick=closeEditor;document.getElementById("assignRackNameBtn")?.addEventListener("click",showNameEditor);document.getElementById("saveRackNameBtn")?.addEventListener("click",saveName);document.getElementById("cancelRackNameBtn")?.addEventListener("click",hideNameEditor);nameInput?.addEventListener("keydown",e=>{if(e.key==="Enter"){e.preventDefault();saveName()}else if(e.key==="Escape"){e.preventDefault();hideNameEditor()}});document.getElementById("deleteRackBtn").onclick=()=>{try{E.removeRack(rackId);persistNow();if(global.parent!==global&&global.parent.document.getElementById("rackEditShell"))global.parent.document.getElementById("closeRackEdit").click();else location.href="workspace-library.html?type=rack"}catch(e){flash(e.message)}};global.MultiSynthRackHandleBack=()=>{if(editing){closeEditor();return true}if(nameEditor&&!nameEditor.classList.contains("hidden")){hideNameEditor();return true}return false};global.addEventListener("pagehide",persistNow);global.addEventListener("beforeunload",persistNow);
})(window);
