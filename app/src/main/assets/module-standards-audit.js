"use strict";
(function(global){
 const MS=global.MultiSynth||{},I=MS.ModuleIds,M=MS.ModuleManifest,C=MS.ModuleContract,K=MS.ModuleCapabilities,S=MS.StateSchema,E=MS.Events,Q=MS.StateKeys,D=MS.ControlDescriptors,B=MS.ModuleBuilderDefinitions;if(!I||!M||!C)return;
 const errors=[],warnings=[],sameList=(a,b)=>JSON.stringify([...(a||[])].sort())===JSON.stringify([...(b||[])].sort()),requiredCaps=["audioInput","audioOutput","generator","noteInput","cvInput","cvOutput","clockSource","clockFollower","dvInput","mic","pcm","midi","terminalOutput"],allowed={audioRole:new Set(["none","generator","processor","passthrough","terminal"]),carrierBehavior:new Set(["none","replace","add","transform","passthrough","moduleSpecific"]),stereoBehavior:new Set(["none","mono","stereoPreserve","monoToStereo","moduleSpecific"]),timingRole:new Set(["none","source","follower","sourceFollower","dividerConsumer","moduleSpecific"]),cvBehavior:new Set(["none","source","trigger","continuous","passthrough","triggerPassthrough","continuousPassthrough","moduleSpecific"]),voiceMode:new Set(["none","mono","poly","moduleSpecific"]),bypassBehavior:new Set(["passthrough","silence","moduleSpecific"])};
 if(!E)errors.push("event registry missing");if(!Q)errors.push("state-key registry missing");else for(const key of ["RUNNING","LEVEL","BPM","STEPS","DV_INPUT"])if(!Q[key])errors.push("shared state key missing "+key);if(!D)errors.push("control descriptor registry missing");else for(const type of ["KNOB","TOGGLE","SELECT","RANGE"])if(!D[type])errors.push("control descriptor type missing "+type);if(!K)errors.push("capability registry missing");else for(const cap of requiredCaps)if(!K.ALL?.includes(cap))errors.push("capability missing "+cap);if(!B)errors.push("module builder registry missing");
 const capHas=(meta,flag)=>!!K?.has(meta,flag),activeIds=M.all.map(x=>x.id),activeSet=new Set(activeIds);for(const id of I.ALL)if(!activeSet.has(id))errors.push("identity has no active manifest: "+id);
 for(const id of activeIds){const ident=I.identityFor(id),meta=M.get(id),builder=B?.get?.(id);if(!ident){errors.push("identity catalog missing "+id);continue}if(!meta){errors.push("manifest missing "+id);continue}if(!builder)errors.push(id+" missing Module Builder definition");else{if(builder.model!=="module-builder")errors.push(id+" Module Builder model invalid");if(Number(builder.stateSchemaVersion||builder.version||0)!==Number(S?.versionFor(id,1)||1))errors.push(id+" Module Builder state schema version mismatch");if(builder.faceplate?.livery&&builder.faceplate.livery!==ident.themeKey)errors.push(id+" Module Builder faceplate theme not identity-owned")}
 if(meta.id!==ident.id)errors.push(id+" manifest id not identity-owned");if(meta.displayName!==ident.displayName)errors.push(id+" manifest displayName not identity-owned");if(meta.themeKey!==ident.themeKey)errors.push(id+" manifest theme not identity-owned");const cap=K?.validate(meta.capabilities);if(cap&&!cap.ok)errors.push(id+" unknown capabilities: "+cap.unknown.join(", "));const rt=meta.routing;if(!rt)errors.push(id+" missing routing metadata");else{for(const key of ["audioRole","carrierBehavior","stereoBehavior","timingRole","cvBehavior","bypassBehavior"])if(!allowed[key].has(rt[key]))errors.push(id+" invalid routing "+key+": "+rt[key]);if(!rt.voiceMode||!allowed.voiceMode.has(rt.voiceMode.mode))errors.push(id+" invalid routing voiceMode");if(!Number.isInteger(rt.latencySamples)||rt.latencySamples<0)errors.push(id+" invalid routing latencySamples")}if(!S?.declared||!Object.prototype.hasOwnProperty.call(S.declared,id))errors.push(id+" missing explicit state schema declaration");else if(!S.versionFor(id))errors.push(id+" invalid state schema version");if(meta.editorUrl&&!/\.html(?:$|[?#])/.test(meta.editorUrl))errors.push(id+" invalid editor URL "+meta.editorUrl);try{const def=C.getDefinition(id);if(def.displayName!==ident.displayName)errors.push(id+" definition displayName not identity-owned");if(def.category!==meta.category)errors.push(id+" category not manifest-owned");if(def.editorUrl!==meta.editorUrl)errors.push(id+" editorUrl not manifest-owned");if(def.selectorClass!==ident.themeKey)errors.push(id+" definition theme not identity-owned");if((def.color??null)!==(meta.color??null))errors.push(id+" color not manifest-owned");if(!sameList(def.resources,meta.resources))errors.push(id+" resources not manifest-owned");if((def.noteOn||def.noteOff||def.panic)&&!capHas(meta,K.NOTE_INPUT))errors.push(id+" exposes note handlers without noteInput capability");if(def.cv&&!capHas(meta,K.CV_INPUT))errors.push(id+" exposes CV handler without cvInput capability");if((def.clockTick||def.clockStart||def.clockStop)&&!(capHas(meta,K.CLOCK_SOURCE)||capHas(meta,K.CLOCK_FOLLOWER)))errors.push(id+" exposes clock handlers without clock capability")}catch(e){errors.push(id+" not registered in ModuleContract")}}
 for(const def of C.listDefinitions())if(!activeSet.has(def.type))errors.push("runtime registered for non-active module "+def.type);for(const def of B?.all?.()||[])if(!activeSet.has(def.id))errors.push("Module Builder definition for non-active module "+def.id);const report=Object.freeze({ok:errors.length===0,errors:Object.freeze(errors),warnings:Object.freeze(warnings),checked:activeIds.length});MS.ModuleStandards=Object.freeze({report,metadata:id=>M.require(id),identity:id=>I.identityFor(id),definition:id=>C.getDefinition(id),builder:id=>B?.require?.(id)||null,describe:id=>Object.freeze({identity:I.identityFor(id),metadata:M.require(id),definition:C.getDefinition(id),builder:B?.get?.(id)||null,stateSchemaVersion:S?.versionFor(id,1)||1})});if(errors.length)console.error("MultiSynth standards audit",report);
})(window);
