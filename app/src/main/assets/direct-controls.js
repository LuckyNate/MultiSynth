"use strict";
(()=>{
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
const decimals=step=>{const s=String(step);return s.includes('.')?s.length-s.indexOf('.')-1:0};
function convertSelect(sel){if(!sel||sel.dataset.directified||sel.closest('.pcmTouchscreen,.deviceScreen'))return;sel.dataset.directified='1';const opts=[...sel.options];if(!opts.length)return;const bank=document.createElement('div');bank.className='choiceBank'+(opts.length>8?' scrollChoiceBank':'');function draw(){bank.innerHTML='';for(const o of opts){const b=document.createElement('button');b.type='button';b.className='choiceButton'+(String(sel.value)===String(o.value)?' active':'');b.textContent=o.textContent;b.onclick=()=>{sel.value=o.value;sel.dispatchEvent(new Event('change',{bubbles:true}));draw()};bank.appendChild(b)}}sel.style.display='none';sel.insertAdjacentElement('afterend',bank);draw();sel.addEventListener('change',draw)}
function convertRange(el){if(!el||el.dataset.knobified)return;const lo=Number(el.min||0),hi=Number(el.max||100),step=Math.abs(Number(el.step)||1),increments=Math.abs((hi-lo)/(step||1));if(increments<=24)return;el.dataset.knobified='1';const label=el.closest('label'),name=(label?.childNodes?.[0]?.textContent||el.id||'CONTROL').trim(),wrap=document.createElement('span'),face=document.createElement('span'),tick=document.createElement('i'),val=document.createElement('b');wrap.className='deviceKnob';face.className='deviceKnobFace';tick.className='deviceKnobTick';val.className='deviceKnobValue';face.append(tick,val);wrap.append(face);el.style.display='none';el.insertAdjacentElement('afterend',wrap);let v=clamp(Number(el.value||lo),lo,hi),held=false,pid=null,startY=0,startX=0,startV=v;const places=decimals(step);function snap(x){return clamp(Math.round((x-lo)/step)*step+lo,lo,hi)}function draw(){v=Number(el.value);const p=(v-lo)/(hi-lo||1),deg=-135+p*270;tick.style.transform=`translateX(-50%) rotate(${deg}deg)`;val.textContent=places?v.toFixed(places):String(Math.round(v));wrap.title=`${name}: ${val.textContent}`}function set(x){const nv=snap(x);if(nv===Number(el.value))return;el.value=String(nv);v=nv;draw();el.dispatchEvent(new Event('input',{bubbles:true}));el.dispatchEvent(new Event('change',{bubbles:true}))}wrap.addEventListener('pointerdown',e=>{e.preventDefault();held=true;pid=e.pointerId;startY=e.clientY;startX=e.clientX;startV=Number(el.value);wrap.classList.add('active');try{wrap.setPointerCapture(pid)}catch(_){}});wrap.addEventListener('pointermove',e=>{if(!held||e.pointerId!==pid)return;e.preventDefault();const dy=startY-e.clientY,dx=Math.abs(e.clientX-startX),fine=dx>80?.02:dx>42?.1:1,units=Math.max(step,(hi-lo)/260)*fine;set(startV+dy*units)});const up=e=>{if(!held||(e?.pointerId!==undefined&&e.pointerId!==pid))return;held=false;pid=null;wrap.classList.remove('active')};wrap.addEventListener('pointerup',up);wrap.addEventListener('pointercancel',up);el.addEventListener('input',draw);draw()}
function convertCheckbox(el){if(!el||el.dataset.switchified)return;el.dataset.switchified='1';const label=el.closest('label'),text=(label?.textContent||el.id||'SWITCH').trim(),b=document.createElement('button');b.type='button';b.className='deviceButton deviceSwitch';const draw=()=>{b.classList.toggle('active',el.checked);b.textContent=`${text}: ${el.checked?'ON':'OFF'}`};b.onclick=()=>{el.checked=!el.checked;el.dispatchEvent(new Event('input',{bubbles:true}));el.dispatchEvent(new Event('change',{bubbles:true}));draw()};el.style.display='none';label?.insertAdjacentElement('afterend',b);draw()}
function scan(root=document){root.querySelectorAll?.('select').forEach(convertSelect);root.querySelectorAll?.('input[type=range]').forEach(convertRange);root.querySelectorAll?.('input[type=checkbox]').forEach(convertCheckbox)}
new MutationObserver(ms=>{for(const m of ms)for(const n of m.addedNodes)if(n.nodeType===1)scan(n)}).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('DOMContentLoaded',()=>scan(),{once:true});setTimeout(()=>scan(),0);
})();
