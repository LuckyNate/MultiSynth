"use strict";
(()=>{
if(!document.querySelector('link[href="console-fit.css"]')){const fit=document.createElement('link');fit.rel='stylesheet';fit.href='console-fit.css';document.head.appendChild(fit)}
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
const decimals=step=>{const s=String(step);return s.includes('.')?s.length-s.indexOf('.')-1:0};
function convertSelect(sel){if(!sel||sel.dataset.directified||sel.closest('.pcmTouchscreen,.deviceScreen'))return;sel.dataset.directified='1';const opts=[...sel.options];if(!opts.length)return;const bank=document.createElement('div');bank.className='choiceBank'+(opts.length>8?' scrollChoiceBank':'');function draw(){bank.innerHTML='';for(const o of opts){const b=document.createElement('button');b.type='button';b.className='choiceButton'+(String(sel.value)===String(o.value)?' active':'');b.textContent=o.textContent;b.onclick=()=>{sel.value=o.value;sel.dispatchEvent(new Event('change',{bubbles:true}));draw()};bank.appendChild(b)}}sel.style.display='none';sel.insertAdjacentElement('afterend',bank);draw();sel.addEventListener('change',draw)}
function convertRange(el){if(!el||el.dataset.knobified||el.dataset.nativeRange==='1')return;const lo=Number(el.min||0),hi=Number(el.max||100),step=Math.abs(Number(el.step)||1),span=Math.max(step,Math.abs(hi-lo));el.dataset.knobified='1';const label=el.closest('label'),name=(label?.childNodes?.[0]?.textContent||el.id||'CONTROL').trim(),wrap=document.createElement('span'),face=document.createElement('span'),tick=document.createElement('i'),val=document.createElement('b');wrap.className='deviceKnob';if(span/step>=250)wrap.classList.add('deviceKnobFine');face.className='deviceKnobFace';tick.className='deviceKnobTick';val.className='deviceKnobValue';face.append(tick,val);wrap.append(face);el.style.display='none';el.insertAdjacentElement('afterend',wrap);let held=false,pid=null,lastX=0,lastY=0;const places=decimals(step);function snap(x){return clamp(Math.round((x-lo)/step)*step+lo,lo,hi)}function draw(){const v=clamp(Number(el.value),lo,hi),p=(v-lo)/(hi-lo||1),deg=-135+p*270;tick.style.transform=`translateX(-50%) rotate(${deg}deg)`;val.textContent=places?v.toFixed(places):String(Math.round(v));wrap.title=`${name}: ${val.textContent}`}function set(x){const nv=snap(x);if(nv===Number(el.value))return;el.value=String(nv);draw();el.dispatchEvent(new Event('input',{bubbles:true}));el.dispatchEvent(new Event('change',{bubbles:true}))}
function movement(dx,dy){const up=Math.max(0,-dy),right=Math.max(0,dx),down=Math.max(0,dy),left=Math.max(0,-dx);return(up+right*.42)-(down+left*.42)}
wrap.addEventListener('pointerdown',e=>{e.preventDefault();held=true;pid=e.pointerId;lastX=e.clientX;lastY=e.clientY;wrap.classList.add('active');try{wrap.setPointerCapture(pid)}catch(_){}});
wrap.addEventListener('pointermove',e=>{if(!held||e.pointerId!==pid)return;e.preventDefault();const dx=e.clientX-lastX,dy=e.clientY-lastY;lastX=e.clientX;lastY=e.clientY;const m=movement(dx,dy);if(!m)return;const sensitivity=span/(wrap.classList.contains('deviceKnobFine')?1450:950);set(Number(el.value)+m*sensitivity)});
const up=e=>{if(!held||(e?.pointerId!==undefined&&e.pointerId!==pid))return;held=false;pid=null;wrap.classList.remove('active');draw()};wrap.addEventListener('pointerup',up);wrap.addEventListener('pointercancel',up);wrap.addEventListener('lostpointercapture',up);el.addEventListener('input',draw);el.addEventListener('change',draw);draw()}
function convertCheckbox(el){if(!el||el.dataset.switchified)return;el.dataset.switchified='1';const label=el.closest('label'),text=(label?.textContent||el.id||'SWITCH').trim(),b=document.createElement('button');b.type='button';b.className='deviceButton deviceSwitch';const draw=()=>{b.classList.toggle('active',el.checked);b.textContent=`${text}: ${el.checked?'ON':'OFF'}`};b.onclick=()=>{el.checked=!el.checked;el.dispatchEvent(new Event('input',{bubbles:true}));el.dispatchEvent(new Event('change',{bubbles:true}));draw()};el.style.display='none';label?.insertAdjacentElement('afterend',b);draw()}
function scan(root=document){root.querySelectorAll?.('select').forEach(convertSelect);root.querySelectorAll?.('input[type=range]').forEach(convertRange);root.querySelectorAll?.('input[type=checkbox]').forEach(convertCheckbox)}
new MutationObserver(ms=>{for(const m of ms)for(const n of m.addedNodes)if(n.nodeType===1)scan(n)}).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('DOMContentLoaded',()=>scan(),{once:true});setTimeout(()=>scan(),0);
})();
