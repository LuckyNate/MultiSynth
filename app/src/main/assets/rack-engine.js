"use strict";
(function(global){
const racks=new Map(),cells=new Map(),listeners=new Map(),connections=new Map();let rackSerial=0,moduleSerial=0,connectionSerial=0;const key=(r,c)=>`${r}:${c}`,clone=v=>v==null?v:JSON.parse(JSON.stringify(v)),id=(p,n)=>`${p}-${Date.now().toString(36)}-${n.toString(36)}`,Contract=()=>global.MultiSynth?.ModuleContract,Events=()=>global.MultiSynth?.Events,Schema=()=>global.MultiSynth?.StateSchema,StateKeys=()=>global.MultiSynth?.StateKeys,Ids=()=>global.MultiSynth?.ModuleIds;
function emit(type,payload){listeners.get(type)?.forEach(fn=>{try{fn(payload)}catch(e){console.error(e)}});const ev=Events(),name=ev?.RACK_ENGINE||"multisynth-rack-engine",detail={type,payload};if(ev?.dispatch)ev.dispatch(name,detail);else global.dispatchEvent(new CustomEvent(name,{detail}))}
function on(type,fn){if(!listeners.has(type))listeners.set(type,new Set());listeners.get(type).add(fn);return()=>listeners.get(type)?.delete(fn)}
function defineModule(def){if(!Contract())throw new Error("ModuleContract must load before RackEngine");return Contract().define(def).type}
function statePatch(patch,state){return StateKeys()?.normalizePatch(clone(patch||{}),state)||clone(patch||{})}
function canonicalType(type){return Ids()?.canonicalId?.(type)||String(type||"")}
function moduleInstance(type,state){type=canonicalType(type);const def=Contract().getDefinition(type),initial=statePatch(state||{},def.defaults);return{id:id(type,++moduleSerial),type,displayName:def.displayName,category:def.category,version:def.version,kind:def.category,enabled:true,stateSchemaVersion:Schema()?.versionFor(type,1)||1,state:Object.assign({},clone(def.defaults),initial)}}
function nodeId(kind,id,port){return`${kind}:${id}:${port}`}
function rackIn(rid){return nodeId("rack",rid,"in")}function rackOut(rid){return nodeId("rack",rid,"out")}function moduleIn(mid){return nodeId("module",mid,"in")}function moduleOut(mid){return nodeId("module",mid,"out")}
function parseNode(ref){const m=/^(rack|module):(.+):(in|out)$/.exec(String(ref||""));return m?{kind:m[1],id:m[2],port:m[3]}:null}
function hasNode(ref){const n=parseNode(ref);if(!n)return false;if(n.kind==="rack")return racks.has(n.id);for(const r of racks.values())if(r.modules.some(m=>m.id===n.id))return true;return false}
function connectNodes(from,to,type="audio"){const a=parseNode(from),b=parseNode(to);if(!a||!b||a.port!=="out"||b.port!=="in")throw new Error("Node connection must be OUT → IN");if(!hasNode(from)||!hasNode(to))throw new Error("Unknown node endpoint");if(from===to)throw new Error("Cannot connect node to itself");const existing=[...connections.values()].find(x=>x.from===from&&x.to===to&&x.type===type);if(existing)return existing.id;const c={id:id("edge",++connectionSerial),from,to,type:String(type||"audio")};connections.set(c.id,c);emit("node-connected",clone(c));changed();return c.id}
function disconnectNodes(connectionId){const c=connections.get(String(connectionId));if(!c)return false;connections.delete(c.id);emit("node-disconnected",clone(c));changed();return true}
function disconnectEndpoint(ref){for(const c of [...connections.values()])if(c.from===ref||c.to===ref)connections.delete(c.id)}
function rackEdges(){const out=[];for(const c of connections.values()){const a=parseNode(c.from),b=parseNode(c.to);if(a?.kind==="rack"&&b?.kind==="rack")out.push({id:c.id,from:a.id,to:b.id,type:c.type})}return out}
function moduleEdges(rackId){const r=need(rackId),ids=new Set(r.modules.map(m=>m.id)),out=[];for(const c of connections.values()){const a=parseNode(c.from),b=parseNode(c.to);if(a?.kind==="module"&&b?.kind==="module"&&ids.has(a.id)&&ids.has(b.id))out.push({id:c.id,from:a.id,to:b.id,type:c.type})}return out}
function syncModuleChain(rackId){const r=need(rackId);for(const c of [...connections.values()]){const a=parseNode(c.from),b=parseNode(c.to);if(a?.kind==="module"&&b?.kind==="module"&&r.modules.some(m=>m.id===a.id)&&r.modules.some(m=>m.id===b.id))connections.delete(c.id)}for(let i=0;i<r.modules.length-1;i++){const a=r.modules[i],b=r.modules[i+1];const c={id:id("edge",++connectionSerial),from:moduleOut(a.id),to:moduleIn(b.id),type:"audio"};connections.set(c.id,c)}}
function addRack(row,col,opts={}){row|=0;col|=0;if(cells.has(key(row,col)))throw new Error(`Rack cell occupied: ${row},${col}`);const rack={id:String(opts.id||id("rack",++rackSerial)),row,col,enabled:opts.enabled!==false,gain:Number.isFinite(opts.gain)?opts.gain:1,modules:[]};racks.set(rack.id,rack);cells.set(key(row,col),rack.id);emit("rack-added",snapshotRack(rack));changed();return rack.id}
function removeRack(rackId){const r=need(rackId);for(const m of r.modules){destroyRuntimeIfPresent(m.id);disconnectEndpoint(moduleIn(m.id));disconnectEndpoint(moduleOut(m.id))}disconnectEndpoint(rackIn(rackId));disconnectEndpoint(rackOut(rackId));cells.delete(key(r.row,r.col));racks.delete(rackId);emit("rack-removed",{id:rackId});changed()}
function moveRack(rackId,row,col){const r=need(rackId);row|=0;col|=0;const k=key(row,col);if(cells.has(k)&&cells.get(k)!==rackId)throw new Error(`Rack cell occupied: ${row},${col}`);cells.delete(key(r.row,r.col));r.row=row;r.col=col;cells.set(k,rackId);emit("rack-moved",snapshotRack(r));changed()}
function need(rackId){const r=racks.get(rackId);if(!r)throw new Error(`Unknown rack: ${rackId}`);return r}function at(row,col){const rid=cells.get(key(row,col));return rid?racks.get(rid):null}
function neighborhood(rackId){need(rackId);const e=rackEdges(),parents=e.filter(x=>x.to===rackId).map(x=>x.from),children=e.filter(x=>x.from===rackId).map(x=>x.to);return{parents:[...new Set(parents)],siblings:[],children:[...new Set(children)]}}
function graph(){return{racks:[...racks.values()].map(snapshotRack),edges:rackEdges(),connections:[...connections.values()].map(clone)}}
function addModule(rackId,type,state,index){const r=need(rackId),m=moduleInstance(type,state),i=Number.isInteger(index)?Math.max(0,Math.min(index,r.modules.length)):r.modules.length;r.modules.splice(i,0,m);syncModuleChain(rackId);emit("module-added",{rackId,module:clone(m),index:i});changed();return m.id}
function removeModule(rackId,moduleId){const r=need(rackId),i=r.modules.findIndex(m=>m.id===moduleId);if(i<0)throw new Error(`Unknown module instance: ${moduleId}`);const[m]=r.modules.splice(i,1);destroyRuntimeIfPresent(m.id);disconnectEndpoint(moduleIn(m.id));disconnectEndpoint(moduleOut(m.id));syncModuleChain(rackId);emit("module-removed",{rackId,moduleId:m.id});changed()}
function moveModule(rackId,moduleId,index){const r=need(rackId),old=r.modules.findIndex(m=>m.id===moduleId);if(old<0)throw new Error(`Unknown module instance: ${moduleId}`);const[m]=r.modules.splice(old,1),next=Math.max(0,Math.min(index|0,r.modules.length));r.modules.splice(next,0,m);syncModuleChain(rackId);emit("module-moved",{rackId,moduleId,index:next});changed()}
function setModuleState(rackId,moduleId,patch){const r=need(rackId),m=r.modules.find(x=>x.id===moduleId);if(!m)throw new Error(`Unknown module instance: ${moduleId}`);const normalized=statePatch(patch,m.state);Object.assign(m.state,normalized);try{Contract().update(moduleId,normalized)}catch(_){}emit("module-state",{rackId,moduleId,state:clone(m.state)})}
function createModuleRuntime(rackId,moduleId,options={}){const r=need(rackId),m=r.modules.find(x=>x.id===moduleId);if(!m)throw new Error(`Unknown module instance: ${moduleId}`);const rackContext={rackId,row:r.row,col:r.col,hasUpstream:false};rackContext.neighbors=()=>({upstream:rackContext.hasUpstream?[rackId]:[]});return Contract().createRuntime(m,Object.assign({},options,{rack:rackContext}))}
function destroyRuntimeIfPresent(moduleId){try{Contract().destroy(moduleId)}catch(_){}}
function snapshotRack(r){return{id:r.id,row:r.row,col:r.col,enabled:r.enabled,gain:r.gain,inputNode:rackIn(r.id),outputNode:rackOut(r.id),modules:clone(r.modules).map(m=>({...m,inputNode:moduleIn(m.id),outputNode:moduleOut(m.id)}))}}
function changed(){emit("graph-changed",graph())}
function executionPlan(){const enabled=[...racks.values()].filter(r=>r.enabled);return enabled.map(r=>{const n=neighborhood(r.id);return{rackId:r.id,row:r.row,col:r.col,parents:n.parents,siblings:[],children:n.children,ladder:r.modules.filter(m=>m.enabled).map(m=>m.id),moduleEdges:moduleEdges(r.id)}})}
function serialize(meta={}){const saved=[...racks.values()].map(r=>{const rack=snapshotRack(r);rack.modules=rack.modules.map(m=>{let state=m.state;try{state=Contract().serialize(m.id)}catch(_){}const type=canonicalType(m.type);const out=Object.assign({},m,{type,state:clone(state),stateSchemaVersion:Schema()?.versionFor(type,1)||1});delete out.inputNode;delete out.outputNode;return out});delete rack.inputNode;delete rack.outputNode;return rack});return JSON.stringify({format:"multisynth-spatial-rack",version:3,routing:"explicit-nodes",meta:clone(meta),racks:saved,connections:[...connections.values()].map(clone)})}
function clear(){for(const r of racks.values())for(const m of r.modules)destroyRuntimeIfPresent(m.id);racks.clear();cells.clear();connections.clear();changed()}
function hydrateModule(m){const out=clone(m||{});out.type=canonicalType(out.type||out.displayName);const def=Contract().getDefinition(out.type),savedVersion=Number(out.stateSchemaVersion)||1,migrated=Schema()?.restore(out.type,clone(out.state||{}),savedVersion)??clone(out.state||{}),normalized=statePatch(migrated,def.defaults);out.displayName=def.displayName;out.category=def.category;out.kind=def.category;out.version=def.version;out.stateSchemaVersion=Schema()?.versionFor(out.type,1)||1;out.state=Object.assign({},clone(def.defaults),normalized);delete out.inputNode;delete out.outputNode;return out}
function migrateSpatialEdges(savedRacks){const byCell=new Map(savedRacks.map(r=>[key(r.row,r.col),r]));for(const r of savedRacks){for(const dc of[-1,0,1]){const child=byCell.get(key((r.row|0)+1,(r.col|0)+dc));if(child){const c={id:id("edge",++connectionSerial),from:rackOut(r.id),to:rackIn(child.id),type:"audio"};connections.set(c.id,c)}}}}
function restore(json){const data=typeof json==="string"?JSON.parse(json):json;if(data?.format!=="multisynth-spatial-rack")throw new Error("Not a MultiSynth spatial rack project");clear();for(const saved of data.racks||[]){const rid=addRack(saved.row,saved.col,{id:saved.id,enabled:saved.enabled,gain:saved.gain}),r=need(rid),restored=[];for(const raw of saved.modules||[]){try{restored.push(hydrateModule(raw))}catch(e){console.error("Rack restore skipped unknown module",raw?.type||raw?.displayName,e)}}r.modules=restored;syncModuleChain(rid)}if(Number(data.version)>=3&&Array.isArray(data.connections)){connections.clear();for(const raw of data.connections){if(!raw?.from||!raw?.to||!hasNode(raw.from)||!hasNode(raw.to))continue;connections.set(String(raw.id||id("edge",++connectionSerial)),{id:String(raw.id||id("edge",++connectionSerial)),from:String(raw.from),to:String(raw.to),type:String(raw.type||"audio")})}}else migrateSpatialEdges(data.racks||[]);changed();return clone(data.meta||{})}
global.MultiSynth=global.MultiSynth||{};global.MultiSynth.RackEngine=Object.freeze({defineModule,addRack,removeRack,moveRack,addModule,removeModule,moveModule,setModuleState,createModuleRuntime,neighborhood,graph,executionPlan,serialize,restore,clear,on,connectNodes,disconnectNodes,moduleEdges,rackIn,rackOut,moduleIn,moduleOut,parseNode,listModuleDefinitions:()=>Contract().listDefinitions(),getRack:rackId=>snapshotRack(need(rackId)),rackAt:(row,col)=>{const r=at(row,col);return r?snapshotRack(r):null}})
})(window);
