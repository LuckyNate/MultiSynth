"use strict";
(function(global){
const MS=global.MultiSynth=global.MultiSynth||{},C=()=>MS.ModuleContract,I=()=>MS.ModuleIds,K=()=>MS.StateKeys;
const modules=new Map(),connections=new Map(),listeners=new Map();let moduleSerial=0,edgeSerial=0;
const mixer={id:"alchemy",channels:{}};
const clone=v=>v==null?v:JSON.parse(JSON.stringify(v));
const id=(p,n)=>`${p}-${Date.now().toString(36)}-${n.toString(36)}`;
const canonical=t=>I()?.canonicalId?.(t)||String(t||"");
function emit(type,payload){listeners.get(type)?.forEach(fn=>{try{fn(payload)}catch(e){console.error(e)}})}
function on(type,fn){if(!listeners.has(type))listeners.set(type,new Set());listeners.get(type).add(fn);return()=>listeners.get(type)?.delete(fn)}
function ref(mid,port){return`module:${mid}:${port}`}
const moduleIn=mid=>ref(mid,"carrier-in"),moduleOut=mid=>ref(mid,"carrier-out"),moduleCvIn=mid=>ref(mid,"cv-in"),moduleCvOut=mid=>ref(mid,"cv-out"),mixerIn=index=>`mixer:alchemy:input-${Math.max(0,index|0)}`;
function parseNode(value){const s=String(value||""),m=/^module:(.+):(carrier-in|carrier-out|cv-in|cv-out)$/.exec(s);if(m)return{kind:"module",id:m[1],port:m[2],signal:m[2].startsWith("cv-")?"cv":"carrier",direction:m[2].endsWith("-out")?"out":"in"};const x=/^mixer:alchemy:input-(\d+)$/.exec(s);return x?{kind:"mixer",id:"alchemy",port:`input-${x[1]}`,index:Number(x[1]),signal:"carrier",direction:"in"}:null}
function snapshotModule(m){return clone(m)}
function graph(){return{standard:"node-graph",modules:[...modules.values()].map(snapshotModule),connections:[...connections.values()].map(clone),mixer:clone(mixer)}}
function addModule(type,state={}){type=canonical(type);const def=C().getDefinition(type),mid=id("node",++moduleSerial),normalized=K()?.normalizePatch?.(clone(state),def.defaults)||clone(state);modules.set(mid,{id:mid,type,displayName:def.displayName,enabled:true,state:Object.assign({},clone(def.defaults),normalized)});emit("graph-changed",graph());return mid}
function compactMixerInputs(){const rows=[...connections.values()].map(e=>({e,p:parseNode(e.to)})).filter(x=>x.p?.kind==="mixer").sort((a,b)=>a.p.index-b.p.index);rows.forEach((x,i)=>{x.e.to=mixerIn(i)});const next={};rows.forEach((_,i)=>{next[i]=mixer.channels[i]||{level:1,mute:false,solo:false}});mixer.channels=next}
function removeModule(mid){mid=String(mid);if(!modules.has(mid))return false;try{C().destroy(mid)}catch(_){}modules.delete(mid);for(const[eid,e]of [...connections]){const a=parseNode(e.from),b=parseNode(e.to);if(a?.id===mid||b?.kind==="module"&&b.id===mid)connections.delete(eid)}compactMixerInputs();emit("graph-changed",graph());return true}
function setModuleState(mid,patch){const m=modules.get(String(mid));if(!m)throw new Error(`Unknown module instance: ${mid}`);const p=K()?.normalizePatch?.(patch||{},m.state)||patch||{};Object.assign(m.state,p);try{C().update(m.id,p)}catch(_){}emit("module-state",{moduleId:m.id,state:clone(m.state),patch:clone(p)});return clone(m.state)}
function setMixerChannel(index,patch={}){index=Math.max(0,index|0);const c=mixer.channels[index]||(mixer.channels[index]={level:1,mute:false,solo:false});if(Object.prototype.hasOwnProperty.call(patch,"level"))c.level=Math.max(0,Math.min(1,Number(patch.level)||0));if(Object.prototype.hasOwnProperty.call(patch,"mute"))c.mute=!!patch.mute;if(Object.prototype.hasOwnProperty.call(patch,"solo"))c.solo=!!patch.solo;emit("graph-changed",graph());return clone(c)}
function connectNodes(from,to,type){const a=parseNode(from),b=parseNode(to);if(!a||!b||a.direction!=="out"||b.direction!=="in")throw new Error("Node connection must be OUT → IN");if(a.signal!==b.signal)throw new Error("Carrier and CV ports cannot be crossed");if(a.kind!=="module"||!modules.has(a.id))throw new Error("Unknown node endpoint");if(b.kind==="module"&&!modules.has(b.id))throw new Error("Unknown node endpoint");if(b.kind==="module"&&a.id===b.id)throw new Error("Cannot connect node to itself");const edgeType=a.signal==="cv"?"cv":"audio";if(type&&type!==edgeType)throw new Error(`Expected ${edgeType.toUpperCase()} connection`);if(b.kind==="mixer"){if(edgeType!=="audio")throw new Error("Alchemy Mixer accepts audio only");if([...connections.values()].some(x=>x.to===to))throw new Error("Alchemy Mixer input already in use")}const existing=[...connections.values()].find(x=>x.from===from&&x.to===to&&x.type===edgeType);if(existing)return existing.id;const e={id:id("edge",++edgeSerial),from,to,type:edgeType};connections.set(e.id,e);if(b.kind==="mixer"&&!mixer.channels[b.index])mixer.channels[b.index]={level:1,mute:false,solo:false};emit("graph-changed",graph());return e.id}
function disconnectNodes(eid){const ok=connections.delete(String(eid));if(ok){compactMixerInputs();emit("graph-changed",graph())}return ok}
function createModuleRuntime(mid,options={}){const m=modules.get(String(mid));if(!m)throw new Error(`Unknown module instance: ${mid}`);return C().createRuntime(m,options)}
function serialize(meta={}){return JSON.stringify({format:"multisynth-node-graph",version:2,meta:clone(meta),modules:[...modules.values()].map(clone),connections:[...connections.values()].map(clone),mixer:clone(mixer)})}
function restore(raw){const data=typeof raw==="string"?JSON.parse(raw):raw;if(data?.format!=="multisynth-node-graph")throw new Error("Not a MultiSynth node graph project");for(const mid of [...modules.keys()])try{C().destroy(mid)}catch(_){}modules.clear();connections.clear();mixer.channels=clone(data.mixer?.channels||{});for(const src of data.modules||[]){const type=canonical(src.type),def=C().getDefinition(type);modules.set(String(src.id),{id:String(src.id),type,displayName:def.displayName,enabled:src.enabled!==false,state:Object.assign({},clone(def.defaults),clone(src.state||{}))})}for(const e of data.connections||[]){const a=parseNode(e.from),b=parseNode(e.to);if(a?.kind!=="module"||!modules.has(a.id)||!b||a.direction!=="out"||b.direction!=="in"||a.signal!==b.signal)continue;if(b.kind==="module"&&!modules.has(b.id))continue;if(b.kind==="mixer"&&a.signal!=="carrier")continue;connections.set(String(e.id||id("edge",++edgeSerial)),{id:String(e.id||id("edge",++edgeSerial)),from:e.from,to:e.to,type:a.signal==="cv"?"cv":"audio"})}compactMixerInputs();emit("graph-changed",graph());return clone(data.meta||{})}
function clear(){for(const mid of [...modules.keys()])try{C().destroy(mid)}catch(_){}modules.clear();connections.clear();mixer.channels={};emit("graph-changed",graph())}
MS.NodeGraphEngine=Object.freeze({graph,on,addModule,removeModule,setModuleState,setMixerChannel,connectNodes,disconnectNodes,createModuleRuntime,serialize,restore,clear,parseNode,moduleIn,moduleOut,moduleCvIn,moduleCvOut,mixerIn,getModule:id=>{const m=modules.get(String(id));return m?clone(m):null},getMixer:()=>clone(mixer)});
})(window);
