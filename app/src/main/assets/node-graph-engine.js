"use strict";
(function(global){
const MS=global.MultiSynth=global.MultiSynth||{},C=()=>MS.ModuleContract,I=()=>MS.ModuleIds,K=()=>MS.StateKeys,B=()=>MS.ModuleBuilderDefinitions;
const modules=new Map(),connections=new Map(),listeners=new Map();let moduleSerial=0,edgeSerial=0;
const clone=v=>v==null?v:JSON.parse(JSON.stringify(v));
const id=(p,n)=>`${p}-${Date.now().toString(36)}-${n.toString(36)}`;
const canonical=t=>I()?.canonicalId?.(t)||String(t||"");
function emit(type,payload){listeners.get(type)?.forEach(fn=>{try{fn(payload)}catch(e){console.error(e)}})}
function on(type,fn){if(!listeners.has(type))listeners.set(type,new Set());listeners.get(type).add(fn);return()=>listeners.get(type)?.delete(fn)}
function ref(mid,port){return`module:${mid}:${port}`}
const moduleIn=mid=>ref(mid,"carrier-in"),moduleOut=mid=>ref(mid,"carrier-out"),moduleCvIn=mid=>ref(mid,"cv-in"),moduleCvOut=mid=>ref(mid,"cv-out"),moduleInput=(mid,index)=>ref(mid,`input-${Math.max(0,index|0)}`),moduleGateOut=(mid,index)=>ref(mid,`gate-out-${Math.max(0,index|0)}`);
function parseNode(value){const s=String(value||""),m=/^module:(.+):(carrier-in|carrier-out|cv-in|cv-out)$/.exec(s);if(m)return{kind:"module",id:m[1],port:m[2],signal:m[2].startsWith("cv-")?"cv":"carrier",direction:m[2].endsWith("-out")?"out":"in"};const x=/^module:(.+):input-(\d+)$/.exec(s);if(x)return{kind:"module",id:x[1],port:`input-${x[2]}`,index:Number(x[2]),signal:"carrier",direction:"in"};const g=/^module:(.+):gate-out-(\d+)$/.exec(s);return g?{kind:"module",id:g[1],port:`gate-out-${g[2]}`,index:Number(g[2]),signal:"cv",direction:"out",gate:true}:null}
const isAlchemy=m=>m?.type===I()?.ALCHEMY_MIXER,isRearranger=m=>m?.type===I()?.REARRANGER,isDynamicInput=m=>isAlchemy(m);
function graph(){return{standard:"node-graph",modules:[...modules.values()].map(clone),connections:[...connections.values()].map(clone)}}
function addModule(type,state={}){type=canonical(type);const def=C().getDefinition(type),mid=id("node",++moduleSerial),normalized=K()?.normalizePatch?.(clone(state),def.defaults)||clone(state);modules.set(mid,{id:mid,type,displayName:def.displayName,enabled:true,state:Object.assign({},clone(def.defaults),normalized)});emit("graph-changed",graph());return mid}
function compactDynamic(mid){const m=modules.get(String(mid));if(!isDynamicInput(m))return;const rows=[...connections.values()].map(e=>({e,p:parseNode(e.to)})).filter(x=>x.p?.id===m.id&&x.p.index!=null).sort((a,b)=>a.p.index-b.p.index);if(isAlchemy(m)){const old=m.state?.channels||{},next={};rows.forEach((x,i)=>{next[i]=clone(old[x.p.index]||old[String(x.p.index)]||{level:1,mute:false,solo:false});x.e.to=moduleInput(m.id,i)});m.state.channels=next;try{C().update(m.id,{channels:clone(next)})}catch(_){}}}
function removeModule(mid){mid=String(mid);if(!modules.has(mid))return false;try{C().destroy(mid)}catch(_){}modules.delete(mid);const affected=new Set();for(const[eid,e]of [...connections]){const a=parseNode(e.from),b=parseNode(e.to);if(a?.id===mid||b?.id===mid){if(b?.id!==mid&&b?.index!=null)affected.add(b.id);connections.delete(eid)}}for(const id of affected)compactDynamic(id);emit("graph-changed",graph());return true}
function hasBpmKnob(m){try{return!!B()?.get?.(m?.type)?.controls?.some(c=>c?.control==="knob"&&c?.state==="bpm")}catch(_){return false}}
function applyState(m,patch){const p=K()?.normalizePatch?.(patch||{},m.state)||patch||{};Object.assign(m.state,p);try{C().update(m.id,p)}catch(_){}emit("module-state",{moduleId:m.id,state:clone(m.state),patch:clone(p)});return p}
function setModuleState(mid,patch){const m=modules.get(String(mid));if(!m)throw new Error(`Unknown module instance: ${mid}`);const p=K()?.normalizePatch?.(patch||{},m.state)||patch||{};if(Object.prototype.hasOwnProperty.call(p,"bpm")&&hasBpmKnob(m)){const bpm=p.bpm;for(const target of modules.values()){if(!hasBpmKnob(target))continue;const tp=target.id===m.id?p:{bpm};applyState(target,tp)}}else applyState(m,p);return clone(m.state)}
function connectNodes(from,to,type){const a=parseNode(from),b=parseNode(to);if(!a||!b||a.direction!=="out"||b.direction!=="in")throw new Error("Node connection must be OUT → IN");if(a.signal!==b.signal)throw new Error("Carrier and CV ports cannot be crossed");if(!modules.has(a.id)||!modules.has(b.id))throw new Error("Unknown node endpoint");if(a.id===b.id)throw new Error("Cannot connect node to itself");if(a.gate&&!isRearranger(modules.get(a.id)))throw new Error("Gate output is not supported by this module");const edgeType=a.signal==="cv"?"cv":"audio";if(type&&type!==edgeType)throw new Error(`Expected ${edgeType.toUpperCase()} connection`);if(b.index!=null){const target=modules.get(b.id);if(!isDynamicInput(target)||edgeType!=="audio")throw new Error("Dynamic carrier input is not supported by this module");if([...connections.values()].some(x=>x.to===to))throw new Error("Dynamic input already in use")}const existing=[...connections.values()].find(x=>x.from===from&&x.to===to&&x.type===edgeType);if(existing)return existing.id;const e={id:id("edge",++edgeSerial),from,to,type:edgeType};connections.set(e.id,e);if(b.index!=null&&isAlchemy(modules.get(b.id))){const m=modules.get(b.id),channels={...(m.state?.channels||{})};if(!channels[b.index])channels[b.index]={level:1,mute:false,solo:false};m.state.channels=channels;try{C().update(m.id,{channels:clone(channels)})}catch(_){}}emit("graph-changed",graph());return e.id}
function disconnectNodes(eid){const e=connections.get(String(eid)),b=e?parseNode(e.to):null,ok=connections.delete(String(eid));if(ok){if(b?.index!=null)compactDynamic(b.id);emit("graph-changed",graph())}return ok}
function createModuleRuntime(mid,options={}){const m=modules.get(String(mid));if(!m)throw new Error(`Unknown module instance: ${mid}`);return C().createRuntime(m,options)}
function serialize(meta={}){return JSON.stringify({format:"multisynth-node-graph",version:3,meta:clone(meta),modules:[...modules.values()].map(clone),connections:[...connections.values()].map(clone)})}
function restore(raw){const data=typeof raw==="string"?JSON.parse(raw):raw;if(data?.format!=="multisynth-node-graph")throw new Error("Not a MultiSynth node graph project");for(const mid of [...modules.keys()])try{C().destroy(mid)}catch(_){}modules.clear();connections.clear();for(const src of data.modules||[]){const type=canonical(src.type),def=C().getDefinition(type);modules.set(String(src.id),{id:String(src.id),type,displayName:def.displayName,enabled:src.enabled!==false,state:Object.assign({},clone(def.defaults),clone(src.state||{}))})}for(const e of data.connections||[]){const a=parseNode(e.from),b=parseNode(e.to);if(!a||!b||!modules.has(a.id)||!modules.has(b.id)||a.direction!=="out"||b.direction!=="in"||a.signal!==b.signal)continue;if(a.gate&&!isRearranger(modules.get(a.id)))continue;if(b.index!=null&&!isDynamicInput(modules.get(b.id)))continue;connections.set(String(e.id||id("edge",++edgeSerial)),{id:String(e.id||id("edge",++edgeSerial)),from:e.from,to:e.to,type:a.signal==="cv"?"cv":"audio"})}for(const m of modules.values())if(isDynamicInput(m))compactDynamic(m.id);emit("graph-changed",graph());return clone(data.meta||{})}
function clear(){for(const mid of [...modules.keys()])try{C().destroy(mid)}catch(_){}modules.clear();connections.clear();emit("graph-changed",graph())}
MS.NodeGraphEngine=Object.freeze({graph,on,addModule,removeModule,setModuleState,connectNodes,disconnectNodes,createModuleRuntime,serialize,restore,clear,parseNode,moduleIn,moduleOut,moduleCvIn,moduleCvOut,moduleInput,moduleGateOut,getModule:id=>{const m=modules.get(String(id));return m?clone(m):null}});
})(window);
