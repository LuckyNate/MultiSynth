"use strict";
(function(){
  const q=new URLSearchParams(location.search),instance=q.get("instance"),P=parent.MultiSynth||{},I=P.ModuleIds,E=P.NodeGraphEngine,A=P.NodeAudioGraph,B=P.ModuleBuilderDefinitions,R=window.MultiSynth?.ControlSurfaceRenderer;
  if(!I||!E||!B||!R)return;
  let state={};if(instance)try{state=E.getModule?.(instance)?.state||{}}catch(_){}
  const def=B.get(I.NO_QUARTER);if(!def)return;const root=document.getElementById("controls");if(!root)return;root.innerHTML="";root.classList.add("ms-module-surface");
  const mainBank=document.createElement("section");mainBank.className="ms-module-bank nq-panel";const mainGrid=document.createElement("div");mainGrid.className="ms-control-grid ms-layout-knobs nq-controls";mainBank.appendChild(mainGrid);root.appendChild(mainBank);
  const crackleBank=document.createElement("section");crackleBank.className="ms-module-bank nq-crackle-bank";crackleBank.innerHTML='<div class="nq-crackle-head"><div class="nq-crackle-title">CRACKLE EVENTS</div><div class="nq-crackle-sub">MASTER VOLUME · EVENT DENSITY</div></div>';const crackleGrid=document.createElement("div");crackleGrid.className="ms-control-grid ms-layout-knobs nq-crackle-grid";crackleBank.appendChild(crackleGrid);root.appendChild(crackleBank);
  // Live knob/dial edits are state updates only. Never rebuild the audio graph here: pointer movement would crackle. Graph rebuilds are structural-only.
  const crackleIds=new Set(["crackle","crackleMicro","crackleTick","cracklePop","crackleDust","crackleArc"]),patch=(key,value)=>{state={...state,[key]:value};if(instance)try{E.setModuleState(instance,{[key]:value})}catch(e){console.error(e)}},clamp=(v,min,max)=>Math.min(max,Math.max(min,v)),quant=(v,min,step)=>min+Math.round((v-min)/step)*step;
  const paint=(node,d,v)=>{const min=Number(d.value?.min??0),max=Number(d.value?.max??1),pct=(v-min)/(max-min||1),angle=-135+clamp(pct,0,1)*270,p=node.querySelector(".ms-control-pointer");if(p)p.style.transform=`translateX(-50%) rotate(${angle}deg)`;const out=node.querySelector(".ms-control-value");if(out){const unit=d.meta?.unit||"";out.textContent=(Number.isInteger(Number(d.value?.step))?Math.round(v):Number(v).toFixed(2))+unit}};
  for(const d0 of def.controls){if(d0.control!=="knob"&&d0.control!=="dial")continue;const d={...d0,meta:{...(d0.meta||{}),visual:{variant:d0.control==="dial"?"rotary":"cap",valueReadout:true}}},target=crackleIds.has(d.id)?crackleGrid:mainGrid;let node;try{node=R.mount(target,d)}catch(e){console.error("NO QUARTER CONTROL RENDER",d.id,e);continue}if(crackleIds.has(d.id)){node.classList.add("nq-crackle-control");if(d.id==="crackle")node.classList.add("nq-crackle-master")}const key=d.state||d.id,min=Number(d.value?.min??0),max=Number(d.value?.max??1),step=Number(d.value?.step??.01);let value=Number(state[key]??d.value?.default??min),startY=0,startValue=value,active=false;paint(node,d,value);node.addEventListener("pointerdown",e=>{active=true;startY=e.clientY;startValue=value;node.setPointerCapture?.(e.pointerId);node.dataset.active="1";e.preventDefault()});node.addEventListener("pointermove",e=>{if(!active)return;value=clamp(quant(startValue+(startY-e.clientY)*(max-min)/180,min,step),min,max);paint(node,d,value);patch(key,value);e.preventDefault()});const end=e=>{if(!active)return;active=false;node.dataset.active="0";try{node.releasePointerCapture?.(e.pointerId)}catch(_){}};node.addEventListener("pointerup",end);node.addEventListener("pointercancel",end)}
  const keyboardHost=document.getElementById("performanceKeyboard");if(keyboardHost&&window.MultiSynth?.PerformanceKeyboard?.mount)window.MultiSynth.PerformanceKeyboard.mount(keyboardHost,{audio:A});
})();