"use strict";
(function(global){
const MS=global.MultiSynth=global.MultiSynth||{},PCM=MS.RawPCMLibrary||MS.PCMLibrary,DB_NAME="multisynth-grain-library",DB_VERSION=1,STORE="grains",ROOT_FOLDER="samples",FOLDER_STORAGE_KEY="multisynth-sample-folders-v3";let dbp=null;
const folderDefs=new Map([[ROOT_FOLDER,{id:ROOT_FOLDER,label:"SAMPLES",order:0,parentId:null,builtin:true}]]);
function sanitizeFolderId(id){return String(id||"").toLowerCase().replace(/[^a-z0-9_-]/g,"")}
function loadFolders(){try{const saved=JSON.parse(global.localStorage?.getItem(FOLDER_STORAGE_KEY)||"[]");if(Array.isArray(saved))for(const raw of saved){const id=sanitizeFolderId(raw?.id);if(!id||id==="root"||id===".."||id===ROOT_FOLDER||folderDefs.has(id))continue;const parentId=raw?.parentId?sanitizeFolderId(raw.parentId):ROOT_FOLDER;folderDefs.set(id,{id,label:String(raw?.label||id.toUpperCase()),order:Number(raw?.order)||500,parentId,builtin:false})}}catch(_){}}
function persistFolders(){try{const custom=[...folderDefs.values()].filter(f=>!f.builtin).map(({id,label,order,parentId})=>({id,label,order,parentId:parentId||ROOT_FOLDER}));global.localStorage?.setItem(FOLDER_STORAGE_KEY,JSON.stringify(custom))}catch(_){}}
loadFolders();
function safeFolder(id){id=sanitizeFolderId(id);return folderDefs.has(id)?id:ROOT_FOLDER}
function createsCycle(id,parentId){let at=parentId;while(at){if(at===id)return true;at=folderDefs.get(at)?.parentId||null}return false}
function registerFolder(def){const id=sanitizeFolderId(def?.id);if(!id||id==="root"||id==="..")throw new Error("Invalid sample folder");const existing=folderDefs.get(id);const parentId=id===ROOT_FOLDER?null:(def?.parentId?safeFolder(def.parentId):(existing?.parentId||ROOT_FOLDER));if(parentId&&!folderDefs.has(parentId))throw new Error("Parent sample folder not found");if(createsCycle(id,parentId))throw new Error("Sample folder cycle");folderDefs.set(id,{id,label:String(def?.label||existing?.label||id.toUpperCase()),order:Number(def?.order)||existing?.order||500,parentId,builtin:!!existing?.builtin});persistFolders();return id}
function createFolder(label,parentId=ROOT_FOLDER){label=String(label||"").trim();if(!label)throw new Error("Folder name required");parentId=safeFolder(parentId);let base=sanitizeFolderId(label)||"folder",id=base,n=2;while(folderDefs.has(id))id=`${base}-${n++}`;folderDefs.set(id,{id,label,order:500,parentId,builtin:false});persistFolders();return{...folderDefs.get(id)}}
function renameFolder(id,label){id=safeFolder(id);const f=folderDefs.get(id);if(!f||f.builtin)return false;label=String(label||"").trim();if(!label)throw new Error("Folder name required");f.label=label;persistFolders();return true}
function moveFolder(id,parentId=ROOT_FOLDER){id=safeFolder(id);const f=folderDefs.get(id);if(!f||f.builtin)return false;parentId=safeFolder(parentId);if(createsCycle(id,parentId))throw new Error("Sample folder cycle");f.parentId=parentId;persistFolders();return true}
async function removeFolder(id){id=safeFolder(id);const f=folderDefs.get(id);if(!f||f.builtin)return false;const children=[...folderDefs.values()].some(x=>x.parentId===id);if(children)throw new Error("Folder contains subfolders");const all=await unifiedList();if(all.some(x=>classify(x)===id))throw new Error("Folder contains samples");folderDefs.delete(id);persistFolders();return true}
function classify(rec){const id=sanitizeFolderId(rec?.folder);return folderDefs.has(id)?id:ROOT_FOLDER}
function open(){if(dbp)return dbp;dbp=new Promise((resolve,reject)=>{const r=indexedDB.open(DB_NAME,DB_VERSION);r.onupgradeneeded=()=>{const db=r.result;if(!db.objectStoreNames.contains(STORE)){const s=db.createObjectStore(STORE,{keyPath:"id"});s.createIndex("createdAt","createdAt");s.createIndex("name","name")}};r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)});return dbp}
function tx(mode,fn){return open().then(db=>new Promise((resolve,reject)=>{const t=db.transaction(STORE,mode),s=t.objectStore(STORE);let result;try{result=fn(s,t)}catch(e){reject(e);return}t.oncomplete=()=>resolve(result);t.onerror=()=>reject(t.error);t.onabort=()=>reject(t.error||new Error("Grain transaction aborted"))}))}
function id(){return"grain-"+Date.now().toString(36)+"-"+Math.random().toString(36).slice(2,9)}
async function save(grain){if(!grain?.data?.length||!grain.sampleRate)throw new Error("Grain requires data and sampleRate");const data=grain.data instanceof Float32Array?grain.data:new Float32Array(grain.data),now=Date.now(),rec={id:grain.id||id(),name:String(grain.name||"GRAIN"),sampleRate:Number(grain.sampleRate),channels:1,frames:data.length,duration:data.length/Number(grain.sampleRate),createdAt:Number(grain.createdAt)||now,updatedAt:now,sourceId:grain.sourceId?String(grain.sourceId):null,sourceName:String(grain.sourceName||""),startFrame:Number(grain.startFrame)||0,endFrame:Number(grain.endFrame)||data.length,tags:Array.isArray(grain.tags)?grain.tags.map(String):[],folder:safeFolder(grain.folder),data};await tx("readwrite",s=>s.put(rec));global.dispatchEvent(new CustomEvent("multisynth-grain-library",{detail:{action:"save",id:rec.id,name:rec.name,folder:rec.folder}}));return{...rec,data:undefined,library:"grain"}}
async function get(id){const db=await open();return new Promise((resolve,reject)=>{const t=db.transaction(STORE,"readonly"),r=t.objectStore(STORE).get(String(id));r.onsuccess=()=>resolve(r.result||null);r.onerror=()=>reject(r.error)})}
async function list(){const db=await open();return new Promise((resolve,reject)=>{const t=db.transaction(STORE,"readonly"),r=t.objectStore(STORE).getAll();r.onsuccess=()=>resolve((r.result||[]).sort((a,b)=>(b.createdAt||0)-(a.createdAt||0)).map(x=>({...x,data:undefined,library:"grain",folder:classify(x),source:"grain-library"})));r.onerror=()=>reject(r.error)})}
async function remove(id){await tx("readwrite",s=>s.delete(String(id)));global.dispatchEvent(new CustomEvent("multisynth-grain-library",{detail:{action:"delete",id:String(id)}}));return true}
async function rename(id,name){const rec=await get(id);if(!rec)return false;rec.name=String(name||rec.name);rec.updatedAt=Date.now();await tx("readwrite",s=>s.put(rec));global.dispatchEvent(new CustomEvent("multisynth-grain-library",{detail:{action:"rename",id:rec.id,name:rec.name}}));return true}
async function setFolder(id,folder){const rec=await get(id);if(!rec)return false;rec.folder=safeFolder(folder);rec.updatedAt=Date.now();await tx("readwrite",s=>s.put(rec));global.dispatchEvent(new CustomEvent("multisynth-grain-library",{detail:{action:"folder",id:rec.id,folder:rec.folder}}));return true}
const Grain=Object.freeze({open,save,get,list,remove,rename,setFolder});
async function unifiedList(){const [pcm,grains]=await Promise.all([PCM?.list?.()||[],list()]);return[...pcm.map(x=>({...x,library:"pcm",folder:classify(x)})),...grains.map(x=>({...x,folder:classify(x)}))].sort((a,b)=>(b.createdAt||0)-(a.createdAt||0))}
async function getUnified(key){if(String(key||"").startsWith("grain-")){const g=await get(key);return g?{...g,library:"grain",folder:classify(g),source:"grain-library"}:null}const p=await PCM?.get?.(key);if(p)return{...p,library:"pcm",folder:classify(p)};const g=await get(key);return g?{...g,library:"grain",folder:classify(g),source:"grain-library"}:null}
async function setUnifiedFolder(key,folder){folder=safeFolder(folder);return String(key||"").startsWith("grain-")?setFolder(key,folder):PCM.setFolder?.(key,folder)}
async function copyToFolder(key,folder){folder=safeFolder(folder);const rec=await getUnified(key);if(!rec?.data?.length)return null;const copyName=String(rec.name||"SAMPLE")+" COPY";if(rec.library==="grain")return save({name:copyName,sampleRate:rec.sampleRate,data:rec.data,sourceId:rec.sourceId,sourceName:rec.sourceName,startFrame:rec.startFrame,endFrame:rec.endFrame,tags:[...(rec.tags||[]),"copy"],folder});return PCM.save({name:copyName,sampleRate:rec.sampleRate,data:rec.data,source:rec.source||"sample-library-copy",tags:[...(rec.tags||[]),"copy"],folder})}
async function folders(){const all=await unifiedList(),counts={};for(const x of all){const f=classify(x);counts[f]=(counts[f]||0)+1}return[...folderDefs.values()].sort((a,b)=>a.order-b.order||a.label.localeCompare(b.label)).map(f=>({...f,count:counts[f.id]||0}))}
const Unified=Object.freeze({
 rootFolder:ROOT_FOLDER,
 list:unifiedList,
 async listFolder(folder){folder=safeFolder(folder);return(await unifiedList()).filter(x=>classify(x)===folder)},
 folders,folderFor:rec=>classify(rec),registerFolder,createFolder,renameFolder,moveFolder,removeFolder,
 get:getUnified,
 save:v=>PCM.save({...v,folder:safeFolder(v?.folder)}),
 saveCapture:(capture,options={})=>PCM.saveCapture(capture,{...options,folder:safeFolder(options?.folder)}),
 remove:key=>String(key||"").startsWith("grain-")?remove(key):PCM.remove(key),rename:(key,name)=>String(key||"").startsWith("grain-")?rename(key,name):PCM.rename(key,name),setFolder:setUnifiedFolder,copyToFolder,open:()=>Promise.all([PCM?.open?.(),open()])
});
MS.RawPCMLibrary=PCM;MS.GrainLibrary=Grain;MS.UnifiedLibrary=Unified;MS.PCMLibrary=Unified;
})(window);