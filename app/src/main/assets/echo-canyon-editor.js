"use strict";
(()=>{const q=new URLSearchParams(location.search),rackId=q.get("rack"),instance=q.get("instance"),P=parent.MultiSynth||{},E=P.RackEngine,A=P.RackAudioGraph,U=window.RackUI;let rack,module;try{rack=E.getRack(rackId);module=rack.modules.find(m=>m.id===instance)}catch(_){}if(module?.type!=="echo-canyon")return;document.documentElement.dataset.moduleType="echo-canyon";document.documentElement.style.setProperty("--accent","#c88945");document.getElementById("title").textContent="ECHO CANYON";document.getElementById("desc").textContent="PHYSICAL-DISTANCE ECHO · CAVE ↔ CANYON REFLECTION FIELD";const root=document.getElementById("controls"),state=module.state||{};root.innerHTML="";function patch(p){Object.assign(state,p);try{E.setModuleState(rackId,instance,p);A?.rebuild?.()}catch(e){console.error(e)}}function group(name){const s=document.createElement("section");s.className="group canyonGroup";const h=document.createElement("h2");h.textContent=name;s.appendChild(h);root.appendChild(s);return s}function range(sec,id,label,min,max,step){return U.range(sec,{value:state[id]??min,min,max,step,label,onInput:v=>patch({[id]:v})})}function toggle(sec,id,label){return U.toggle(sec,{value:!!state[id],label,onChange:v=>patch({[id]:v})})}
const vista=document.createElement("section");vista.className="canyonVista";vista.innerHTML='<div class="canyonSky"></div><div class="canyonWall left"></div><div class="canyonWall right"></div><div class="canyonFloor"></div><div class="canyonMarker">SOURCE</div><div class="canyonDistance" id="canyonDistance"></div>';root.appendChild(vista);const distanceLabel=vista.querySelector("#canyonDistance");function paintDistance(){const d=Number(state.distance??45);distanceLabel.textContent=`REFLECTOR ${d.toFixed(0)} m · FIRST RETURN ${(2*d/343*1000).toFixed(0)} ms`}
const geometry=group("GEOMETRY");const d=range(geometry,"distance","DISTANCE TO WALL · METERS",1,600,1);d.addEventListener("input",paintDistance);range(geometry,"environment","CAVE ← SHAPE → CANYON",0,1,.01);range(geometry,"reflections","REFLECTIONS",1,8,1);paintDistance();const air=group("AIR / ROCK");range(air,"decay","RETURN DECAY",.05,.95,.01);range(air,"damping","AIR / ROCK DAMPING HZ",500,18000,50);range(air,"width","STEREO WIDTH",0,1,.01);const mix=group("PEDAL");range(mix,"mix","ECHO MIX",0,1,.01);range(mix,"level","OUTPUT LEVEL",0,2,.01);toggle(mix,"bypass","BYPASS")})();