"use strict";(()=>{const canvas=document.getElementById("ribbonScope");if(!canvas)return;const q=new URLSearchParams(location.search),instance=q.get("instance"),P=parent.MultiSynth||{},C=P.ModuleContract;let analyser=null,data=null,ctx=canvas.getContext("2d");function resize(){const dpr=Math.max(1,Math.min(2,devicePixelRatio||1)),r=canvas.getBoundingClientRect(),w=Math.max(1,Math.round(r.width*dpr)),h=Math.max(1,Math.round(r.height*dpr));if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h}}function getAnalyser(){if(analyser)return analyser;try{analyser=C?.getRuntime?.(instance)?.user?.analyser||null}catch(_){}if(analyser)data=new Uint8Array(analyser.fftSize);return analyser}function draw(){resize();const w=canvas.width,h=canvas.height;ctx.clearRect(0,0,w,h);ctx.fillStyle="#010101";ctx.fillRect(0,0,w,h);ctx.strokeStyle="#18202a";ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(0,h*.5);ctx.lineTo(w,h*.5);ctx.stroke();const a=getAnalyser();if(a){if(!data||data.length!==a.fftSize)data=new Uint8Array(a.fftSize);a.getByteTimeDomainData(data);ctx.strokeStyle="#f4f8ff";ctx.lineWidth=Math.max(1,devicePixelRatio||1);ctx.beginPath();for(let i=0;i<data.length;i++){const x=i/(data.length-1)*w,y=(data[i]/255)*h;(i?ctx.lineTo(x,y):ctx.moveTo(x,y))}ctx.stroke()}requestAnimationFrame(draw)}draw()})();