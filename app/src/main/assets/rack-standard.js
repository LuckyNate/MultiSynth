"use strict";
/* Compatibility shim only. RackStandard is defined once by module-contract.js. */
(function(global){if(!global.MultiSynth?.RackStandard)console.warn("RackStandard unavailable: load module-contract.js first");})(window);
