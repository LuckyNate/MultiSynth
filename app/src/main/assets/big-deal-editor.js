"use strict";
(()=>{
const q=new URLSearchParams(location.search),instance=q.get("instance"),P=parent.MultiSynth||{},I=P.ModuleIds,E=P.NodeGraphEngine,A=P.NodeAudioGraph,L=P.UnifiedLibrary||P.PCMLibrary,B=P.ModuleBuilderDefinitions,R=window.MultiSynth?.ControlSurfaceRenderer;if(!instance||!I||!E||!B||!R)return;const module=E.getModule(instance),model=B.get(I.BIG_DEAL);if(!module||!model)return;let state={...model.defaults,...(module.state||{})};const root=document.getElementById("controls");root.innerHTML="";root.classList.add("ms-module-surface");
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v)),patch=p=>{state={...state,...p};E.setModuleState(instance,p);A?.rebuild?.()},mount=(p,d,v={})=>R.mount(p,{...d,meta:{...(d.meta||{}),visual:{...(d.meta?.visual||{}),...v}}});function bank(title,layout="ms-layout-list"){const s=document.createElement("section");s.className="ms-module-bank";const h=document.createElement("div");h.className="ms-module-bank-title";h.textContent=title;const g=document.createElement("div");g.className=`ms-control-grid ${layout}`;s.append(h,g);root.appendChild(s);return g}
function button(parent,id,label,fn,active=false){const n=mount(parent,{id,control:"button",label},{variant:"rect"});n.dataset.active=active?"1":"0";n.onclick=fn;return n}
function toggle(parent,id,label){let value=!!state[id];const n=mount(parent,{id,control:"switch",state:id,label,value:{default:value}},{variant:"rocker"}),paint=()=>{n.dataset.on=value?"1":"0";n.dataset.active=value?"1":"0"};n.onclick=()=>{value=!value;paint();patch({[id]:value})};n.setModuleValue=v=>{value=!!v;paint()};paint();return n}
function knob(parent,id,label,min,max,step,unit=""){let value=Number(state[id]??min),active=false,startY=0,startValue=value;const n=mount(parent,{id,control:"knob",state:id,label,value:{default:value,min,max,step},meta:{unit}},{variant:"cap",valueReadout:true}),p=n.querySelector(".ms-control-pointer"),out=n.querySelector(".ms-control-value"),quant=v=>clamp(min+Math.round((v-min)/step)*step,min,max),paint=()=>{const t=(value-min)/(max-min||1);if(p)p.style.transform=`translateX(-50%) rotate(${-135+t*270}deg)`;if(out)out.textContent=`${step<1?value.toFixed(Math.min(3,(String(step).split('.')[1]||'').length)):Math.round(value)}${unit}`};n.onpointerdown=e=>{active=true;startY=e.clientY;startValue=value;n.setPointerCapture?.(e.pointerId);e.preventDefault()};n.onpointermove=e=>{if(!active)return;const next=quant(startValue+(startY-e.clientY)*(max-min)/180);if(next!==value){value=next;paint();patch({[id]:value})}e.preventDefault()};const end=e=>{active=false;try{n.releasePointerCapture?.(e.pointerId)}catch(_){}};n.onpointerup=end;n.onpointercancel=end;n.setModuleValue=v=>{value=quant(Number(v));paint()};paint();return n}
function selector(parent){const d=model.controls.find(x=>x.id==="mode"),opts=d.value.options;let idx=Math.max(0,opts.indexOf(state.mode)),active=false,startY=0,start=idx;const n=mount(parent,{id:"mode",control:"dial",state:"mode",label:"DEAL",value:{default:idx,min:0,max:opts.length-1,step:1}},{variant:"indexed",valueReadout:true}),p=n.querySelector(".ms-control-pointer"),out=n.querySelector(".ms-control-value"),paint=()=>{const t=idx/(opts.length-1||1);if(p)p.style.transform=`translateX(-50%) rotate(${-135+t*270}deg)`;if(out)out.textContent=String(opts[idx]).toUpperCase()};n.onpointerdown=e=>{active=true;startY=e.clientY;start=idx;n.setPointerCapture?.(e.pointerId);e.preventDefault()};n.onpointermove=e=>{if(!active)return;const next=clamp(Math.round(start+(startY-e.clientY)/28),0,opts.length-1);if(next!==idx){idx=next;paint();patch({mode:opts[idx]})}};const end=e=>{active=false;try{n.releasePointerCapture?.(e.pointerId)}catch(_){}};n.onpointerup=end;n.onpointercancel=end;paint()}
let armed="sourceA";const deckBank=bank("DECK SCREEN","ms-layout-list"),screen=mount(deckBank,{id:"deck-screen",control:"screen",label:"PCM + GRAIN LIBRARIES"},{variant:"screen",height:300}),face=screen.querySelector(".ms-control-face"),slotGrid=document.createElement("div"),lib=document.createElement("div");slotGrid.className="bigDealSlots";lib.className="bigDealLibrary";face.append(slotGrid,lib);
async function nameFor(id){const r=id&&L?await L.get(id):null;return r?.name||"EMPTY"}async function drawDeck(){slotGrid.innerHTML="";for(const [key,label] of [["sourceA","DECK A"],["sourceB","DECK B"],["sourceC","DECK C"],["sourceD","DECK D"]]){const n=await nameFor(state[key]),b=button(slotGrid,`slot-${key}`,`${label} · ${n}`,()=>{armed=key;drawDeck();drawLibrary() },armed===key);b.classList.add("bigDealSlot")}}
async function drawLibrary(){lib.innerHTML="";const items=L?await L.list():[];for(const rec of items){const row=document.createElement("div");row.className="ms-list-row";lib.appendChild(row);const b=button(row,`load-${rec.id}`,`${rec.name} · ${(rec.duration||0).toFixed(3)}s`,()=>{patch({[armed]:rec.id});drawDeck();drawLibrary();if(armed==="sourceA")drawSeek()});b.classList.add("bigDealLibraryItem")}}
const params=bank("DEALER CONTROLS","ms-layout-params");selector(params);knob(params,"cardMs","CARD SIZE",10,10000,1," ms");knob(params,"dealGps","DEAL RATE",1,100,1," gps");knob(params,"pitch","PITCH",-24,24,1," st");knob(params,"stretch","STRETCH",.25,8,.05,"×");knob(params,"level","LEVEL",0,1,.01);const actions=bank("ACTIONS","ms-layout-transport");toggle(actions,"timed","TIMED");button(actions,"randomize","RANDOMIZER",()=>patch({randomize:true}));button(actions,"preview","AUDITION OUTPUT",()=>patch({preview:true}));button(actions,"saveOutput","SAVE OUTPUT",()=>patch({saveOutput:true}));
const seekBank=bank("SEEK / GRAIN","ms-layout-list"),seekWrap=document.createElement("div");seekWrap.className="bigDealSeek";seekBank.appendChild(seekWrap);function nextZero(data,from){let i=Math.max(1,Math.min(data.length-1,Math.ceil(from)));for(;i<data.length;i++){const a=data[i-1],b=data[i];if(a===0||b===0||(a<0&&b>0)||(a>0&&b<0))return i}return data.length}async function countCards(){const rec=state.sourceA&&L?await L.get(state.sourceA):null;if(!rec?.data?.length)return 1;const min=Math.max(1,Math.ceil(rec.sampleRate*Math.max(10,Number(state.cardMs)||20)/1000));let at=0,count=0;while(at<rec.data.length){let end=nextZero(rec.data,at+min);if(end<=at)end=Math.min(rec.data.length,at+min);count++;at=end}return Math.max(1,count)}async function drawSeek(){seekWrap.innerHTML="";const count=await countCards(),grid=document.createElement("div"),buttons=document.createElement("div");grid.className="ms-control-grid ms-layout-params";buttons.className="ms-control-grid ms-layout-transport";seekWrap.append(grid,buttons);state.seekIndex=clamp(Math.round(Number(state.seekIndex)||0),0,count-1);state.seekLength=clamp(Math.round(Number(state.seekLength)||1),1,count-state.seekIndex);knob(grid,"seekIndex","START",0,count-1,1);knob(grid,"seekLength","CARDS",1,Math.max(1,count-state.seekIndex),1);knob(grid,"seekFrequency","REPEAT FREQ",1,2000,1," Hz");knob(grid,"seekTime","REPEAT TIME",.01,30,.01," s");button(buttons,"seekPreview","START SEEK",()=>patch({seekPreview:true}));button(buttons,"seekStop","STOP SEEK",()=>patch({seekPreview:false}));button(buttons,"saveGrain","SAVE GRAIN",()=>patch({saveGrain:true}));button(buttons,"saveSeekSample","SAVE SAMPLE",()=>patch({saveSeekSample:true}))}
drawDeck().catch(console.error);drawLibrary().catch(console.error);drawSeek().catch(console.error);window.addEventListener("multisynth-state-sync",e=>{state={...model.defaults,...(e.detail||state)};root.querySelectorAll("[data-state-key]").forEach(n=>{const k=n.dataset.stateKey;if(k in state)n.setModuleValue?.(state[k])})});
})();